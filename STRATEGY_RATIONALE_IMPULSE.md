# impulse-bot: удар на Bybit linear perp-альтах

Полный автомат. Не импортирует `scalp_bot` / `horizon_bot`. Параметры из
форумных постов (скрин), не из наших бэктестов и не из P&L scalp.

| Правило | Значение | Источник |
|---|---|---|
| Вселенная | не BTC/ETH/SOL, оборот $100k–$15M | Bitcointalk topic=5577812 |
| Удар | ≥$30k за ~15с и ход ≥0.2% | тот же тред (Dzhango) |
| Подтверждение | лента + кластер, оба | CScalp методичка (стакан/лента/кластеры, не RSI) |
| Сессия | 07–16 UTC | ForexFactory 1014708 (Лондон); Smart-lab 963593 1–2 ч/день |
| Тейк / стоп | 0.45% / 0.25% | FF: не микро-тейк размером с комиссию; RT 0.128% замерен → тейк 3.5× |
| Scratch | 90с если импульс не пошёл | операционный, в постах «быстрый скальп» |
| Плечо / риск | 10×, 1.5% equity | в журналах 20× руками; автомат ниже |
| Исполнение | Market + биржевой SL/TP; лот ≤ `maxMktOrderQty` | CScalp «заброс в край» ≈ тейкер; лимит Market — Bybit instruments-info |

## Комиссия: замер вместо предположения

Изначально тейк обосновывался ставкой VIP 0 RT **0.110%** («тейк ~4×»).
Фактическая ставка на demo — **0.0642% на сторону, 0.128% за круг**:
замер по `execFee` из execution list, 160 филлов / оборот $936 109 за
2026-08-21…31 (`scripts/collect_impulse_stats.py --days 10`).

Следствие: тейк 0.45% — это **3.5×** комиссии, а не 4×. Порог остаётся
прежним, но запас над издержками меньше заявленного, и любое обрезание
сделки до цели быстро уводит её в минус. Параметры TP/SL не менялись.

Лента: `GET /v5/market/recent-trade`
(https://bybit-exchange.github.io/docs/v5/market/recent-trade).
Кластер: доля объёма в крайнем из 5 бинов по ходу удара, порог 0.30 —
дискретизация ленты, не подобранный бэктест.

Чужую позицию на общем демо (scalp / daytrend / swing) не трогает.
`orderLinkId` префикс `impulse_`.
