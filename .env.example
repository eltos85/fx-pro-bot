# Копируйте в .env при необходимости. Секреты брокера не нужны.

DATA_DIR=data
# stub — синтетические цены; yfinance — котировки Yahoo (нужен пакет: pip install 'fx-pro-bot[quotes]')
DATA_SOURCE=stub

YFINANCE_SYMBOL=EURUSD=X
YFINANCE_PERIOD=1mo
YFINANCE_INTERVAL=1h

DISPLAY_NAME=EUR/USD
