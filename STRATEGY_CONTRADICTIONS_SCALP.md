# Противоречия в логике scalp_bot — реестр для разбора

Назначение: фиксировать **внутренние противоречия** между значениями/формулами/
философиями стратегий (источник — `STRATEGY_RATIONALE_SCALP.md`) и **накапливать
по ним исследования**, пока не придём к решению. Это рабочий документ: добавляем
противоречия и складываем сюда research/A-B, на основании которых принимаем
вердикт.

## Как пользоваться

- Каждое противоречие — отдельная запись с ID (`C-NN`).
- Статусы: `🔴 открыто` → `🟡 в работе (копим research)` → `🟢 решено` →
  `⚪ принято как by-design` (намеренное, не баг).
- В «Исследования» складываем ссылки на канон, артефакты `data/`, A/B-логи,
  цитаты. Каждая запись с датой и тегом источника (`[research]`/`[наш A/B]`).
- Менять торговую логику по итогам — только по правилам (strategy-guard.mdc,
  sample-size.mdc, no-data-fitting.mdc): research/данные → вердикт → правка.

## Шаблон новой записи

```
### C-NN: краткое название
**Статус**: 🔴 открыто
**Где**: param/формула A (страта) ↔ param/формула B (страта)
**Суть**: в чём конфликт (2-3 строки).
**Почему важно**: что ломается / какой риск.

**Исследования** (накапливаем):
- [YYYY-MM-DD] [research|наш A/B] источник/артефакт → что показал.

**Вердикт**: (пусто, пока не решено)
```

---

## C-01: ширина SL — комиссия против шума

**Статус**: 🟡 в работе (копим research)
**Где**: `min_risk_fee_mult = 4.0` (общий, мин-R пол) ↔
`sweep_fade_sl_risk_mult = 1.5` (sweep_fade, v0.18.6).
**Суть**: мин-R пол задаёт ширину SL из СТОИМОСТИ КОМИССИИ (R ≥ 4× round-trip,
fee ≤ 0.25R). Множитель ×1.5 раздвигает SL уже из ШУМА рынка (MAE). Две разные
логики управляют одной величиной; ×1.5 фактически признаёт, что комиссионный
пол ставит стоп не туда.
**Почему важно**: если истинный драйвер ширины — шум (MAE), то комиссионный пол
может быть неправильной первичной привязкой; возможно дублирование/конфликт при
тюнинге. Риск двойного учёта или отмены одного другим.

**Исследования** (накапливаем):
- [2026-06-04] [research] MAE/Sweeney 1996 — стоп по реальному ходу против входа,
  не по косту. MR: тугой стоп (1 ATR) убивает edge (aligrithm 3.27 2026).
- [2026-06-04] [наш A/B] 117 стоп-вылетов (`/tmp/sf_rescue.py`): ×1.5 → ~28%
  выбиваний шумовые; харнес by-side май/июнь — из минуса в плюс.
- [ограничение] данные пересекаются с тюнинг-окном фильтров → нужен форвард.
- [2026-06-05] [наш A/B] форвард ×1.5 прерван: деплой попал на тренд-день
  (ADX 29–42), sweep_fade блокировался HTF/ADX, исполнилось n=10 (шум). ОТКАЧЕН
  на 1.0 (v0.18.7). Побочно вскрылся под-вопрос: ×1.5 сдвигает порог flow_exit
  на 2.25R (1.5×1.5) → возможный пропуск фиксаций (на n=10 не подтверждён).

**Вердикт**: (открыт — повторить форвард ×1.5 на НОРМАЛЬНОМ режиме, не тренд-дне;
при возврате — рассмотреть отвязку flow_exit-лока от раздутого риска. Решить,
что первично: комиссия или MAE)

---

## C-02: ADX≥25 — для одной страты «стоп», для другой «можно»

**Статус**: ⚪ принято как by-design (требует подтверждения данными)
**Где**: `htf_adx_gate`/`htf_adx_max=25` блокирует sweep_fade и density_bounce ↔
`regime_gated=False` у density_break (ADX-гейт не применяется).
**Суть**: одно значение силы тренда (ADX≥25 = «established trend») для
mean-reversion = запрет (не фейдить тренд), для momentum-пробоя = желаемое
условие (пробою нужен тренд). Трактовка прямо обратная.
**Почему важно**: это намеренная асимметрия (MR vs momentum), НЕ баг — но
держим под наблюдением: реально ли density_break лучше при ADX≥25, а MR хуже.
Если данные не подтвердят — асимметрия станет догадкой.

**Исследования** (накапливаем):
- [2026-06-04] [research] MR: «never fade a trending market» (Connors/Raschke
  1995). Momentum/breakout хочет тренда (Quant Signals: трендовый фильтр режет
  profitable пробои).
- [2026-06-04] [наш A/B] sweep_fade ema+adx@25 +0.140R vs +0.122R EMA
  (`data/scalp_adx_gate.txt`). [пробел] для density_break разбивки по ADX-корзинам
  ПОКА НЕТ — нужно подтвердить, что пробой реально лучше при ADX≥25.

**Вердикт**: by-design; ПОДТВЕРДИТЬ разбивкой density_break по ADX-корзинам на
≥100 сделок (сейчас n мал).

---

## C-03: выходная механика — flow_exit/scratch против голого TP/SL

**Статус**: ⚪ принято как by-design
**Где**: sweep_fade (`flow_exit`/`flow_scratch` по флипу ленты) ↔
density_break (`should_exit = None`, только биржевые TP/SL).
**Суть**: sweep_fade активно управляет выходом по ордер-флоу; density_break
ведётся голым TP=2.5R/SL. Разные философии выхода для двух страт одного бота.
**Почему важно**: by-design (Философия B для пробоя — winners run без дискреции;
flow_exit придуман под фейд). Но: density_break может терять прибыль на
разворотах перед TP (тот же мотив, что снизил его TP до 2.5R) — возможно ему
тоже нужен flow-выход после валидации базового эджа.

**Исследования** (накапливаем):
- [2026-06-04] [research/no-data-fitting] flow-выход для density_break — отдельная
  итерация ПОСЛЕ валидации базового эджа (чтобы не оверфитить).
- [2026-06-04] [наш A/B] контрфактуал TP density_break: пробои разворачивались с
  2.5–3.4R до 3.5R (#973/#977/#661) → снизили TP до 2.5R (`q_db_mfe`, n=25, шум).

**Вердикт**: by-design сейчас; пересмотреть flow-выход для density_break на ≥100
сделок (тот же разворот-до-TP мотив).

---

## Кандидаты на проверку (ещё не оформлены как противоречия)

- TP 3.5R (sweep_fade) при медиане холда ~76с до SL и flow_exit-локе на 1.5R:
  реально ли сделки доходят до 3.5R, или 3.5R — недостижимая цель и работает
  только flow_exit? (проверить распределение реализованных R).
- `take_profit_r=3.5` + `sweep_fade_sl_risk_mult=1.5`: TP масштабируется вместе
  с SL → фактический TP в цене = 3.5 × 1.5 = 5.25× исходного R. Не делает ли это
  цель недостижимой? (проверить MFE на новых данных).
- `min_target_fee_mult=3.0` (reward-gate) ↔ `flow_exit_activate_r=1.5` (фиксируем
  на 1.5R): сигнал требует потенциал ≥3× кост до TP, а реально часто фиксируем
  на 1.5R по флипу — согласованы ли пороги?
