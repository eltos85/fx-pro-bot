# Build Log — impulse-bot

Изолирован от `scalp_bot` / `horizon_bot` / `flowzone`. Bybit linear, демо.

## 2026-08-31

### research(impulse): убыток детерминирован комиссией, сигнал неотличим от нуля

Повторный замер той же выборки (80 сделок, 2026-08-21..31, demo) по
методологии скилла `trade-forensics`. Торговая логика не менялась,
изменён только измерительный скрипт.

**Что добавлено в замер.** Фильтр `execType` (в статистику стратегии
нельзя мешать `BustTrade` / `Settle` / `MovePosition`, см.
https://bybit-exchange.github.io/docs/v5/position/close-pnl) — отброшено
0 записей, выборка была чистой. Комиссия теперь берётся из `openFee` +
`closeFee` самой записи закрытия и сверяется с суммой `execFee` по
филлам: $600.64 против $600.64, расхождение 0.0%. Добавлены разбивка
long/short, разбивка по дням и `fillCount` в проверку согласованности
(80 = 80 = 80/80 филлов = 80 fillCount).

**Главный результат — t-тест по gross.** Результат без комиссии
−$37.23 за 80 сделок (−$0.47 на сделку) от нуля статистически
неотличим: t = −0.27, p = 0.7861. Net-убыток при этом значим:
t = −4.49, p < 0.0001. Комиссия — 94% убытка ($600.64 из $637.87) и
единственная систематическая составляющая. Вывод: сигнал не
отрицательный, а нулевой; убыток создаётся не входами, а стоимостью
круга 0.1283% при среднем ходе сделки −0.0068%.

**Уточнение прошлых чисел.** Break-even WR пересчитан в долларах вместо
процентов хода (сделки различаются нотионалом): без комиссии нужно
40.4% при факте 36.2% (разрыв 4.2 п.п.), с комиссией — 56.0% при факте
28.7% (разрыв 27.3 п.п.). Прошлая запись давала 37.6% / 65.0% по
процентному расчёту — долларовый точнее. Винрейт разделён на два: по
цене 36.2% (29 сделок), по деньгам 28.7% (23 сделки), комиссия
переводит 6 сделок из плюса в минус.

**Прочее из замера.** Прибыльный день 1 из 10 — минус воспроизводится
ежедневно, не выброс. Long: n=35, WR 34.3%, ход +0.0091%; short: n=45,
WR 24.4%, ход −0.0191% — обе выборки ниже порога, это наблюдение. До
тейка дошли 10% сделок, до стопа 21%, между уровнями закрыты 69%.

**Пороги `sample-size.mdc` не взяты:** 80 сделок из 100, 1.4 недели из
2, счёт демонстрационный. Стратегия не меняется.

**Файлы:** `scripts/collect_impulse_stats.py`

### feat(impulse): снимок сигнала в БД и учёт по фактическим ценам

Торговая логика не менялась: пороги удара, ленты, кластера, TP/SL, scratch
и лимиты те же. Обе правки — про учёт, обе понадобились после разбора
убытков (см. запись ниже).

**Симптом → причина → решение (учёт цен).** Разбор показал «стоп
проскальзывает до −1.5%», хотя по API проскальзывания нет (SL заявлен
−0.25%, факт −0.248%). Причина: `_manage` писал в БД `last_price` в момент,
когда цикл заметил закрытие, то есть до 15с спустя, а вход — цену тикера
на момент решения, а не цену филла Market. На выборке за 2026-08-21..31
это завышало долю прибыльных сделок с 36% до 41%. Теперь рядом со старыми
полями пишутся `entry_real` (avgPrice позиции после входа), `exit_real` и
`pnl_net` (`avgExitPrice` / `closedPnl` из `get_closed_pnl` — уже net с
комиссиями и фандингом). Старые поля оставлены как запасной вариант, если
биржа не отдала запись.

**Зачем снимок сигнала.** Разбор не смог проверить связь «сильнее удар →
лучше исход»: `burst_usd`, `move_pct`, лента и кластер нигде не
сохранялись. Добавлен `SignalSnapshot`, который пишется в `positions` при
входе и переезжает в `trades` при закрытии. Собирается уже ПОСЛЕ того как
`should_enter` вернул True, на решение не влияет.

`last_closed_trade` отсекает чужие записи по `not_before_ts`: на общем демо
тем же символом мог торговать другой бот.

Миграция схемы через `ALTER TABLE ADD COLUMN`, проверена на копии боевой
БД прямо на VPS: 104 записи и 13 дней сессий целы, 9 колонок добавлено,
повторное открытие идемпотентно, запись новой сделки поверх работает.

`scripts/collect_impulse_stats.py` дополнен разбивкой исхода по силе
сигнала — включится сама, когда накопятся сделки с новыми полями.

**Файлы:** `src/impulse_bot/db.py`, `client.py`, `app/main.py`,
`scripts/collect_impulse_stats.py`, `tests/test_impulse_bot.py` (+4 теста:
перенос снимка, раздельные цены, закрытие без данных биржи, миграция)

### research(impulse): почему винрейт 36%, и что его не объясняет

Наблюдение, кода стратегии не касается. Вопрос был «где теряется винрейт».

**Артефакты:** `scripts/_impulse_loss_anatomy.py` (минутные свечи вокруг
каждой сделки), `scripts/_impulse_price_audit.py` (сверка цен БД против
фактических исполнений).
**Выборка:** те же 80 сделок 2026-08-21..31, 1.4 недели.

**Что проверено и не подтвердилось** (U-тест, сравнение прибыльных и
убыточных по признакам, видимым в момент входа):
- поздний вход, ход за 5 мин до входа: +0.361% против +0.400%, p=0.86
- шумность инструмента, размах минуты: 0.206% против 0.195%, p=0.37
- спокойные против шумных инструментов: 40% против 42% в плюсе, p=0.79
- сторона: Buy 43% против Sell 40% в плюсе
- проскальзывание на биржевых уровнях: медиана +0.002 п.п., его нет
- удержание вместо scratch: ожидание +0.021% против нынешних +0.024%
- ширина стопа: перебор 8 комбинаций 0.25…0.75% × 0.45/0.70%, все
  отрицательны, текущая связка в лучшей половине

**Единственное различие — постфактум:** медианный MFE у убыточных +0.117%
(меньше комиссии 0.128%), у прибыльных +0.510%. 15 из 47 убыточных (32%)
ни разу не были выше +0.05%; до тейка доходили и разворачивались 4 из 47.

**Арифметика:** по фактическим ценам WR 36.2%, средний плюс +0.292%,
средний минус −0.176%, R:R 1.65. Безубыток без комиссии требует WR 37.6%
(почти достигнут), с комиссией 0.128% — WR 65.0% либо средний плюс
0.665% (в 2.3 раза больше). Разрыв не закрывается настройкой порогов.

**Побочное наблюдение:** 58 из 80 входов в 07:00 UTC, 18 в 08:00, 4 в
09:00 — дневной лимит `max_trades_session=8` выбирается за первый час,
и остальные 8 часов сессии 07–16 UTC бот не торгует.

Вердикт по `sample-size.mdc`: 80 < 100 сделок, 1.4 < 2 недели — пороги,
scratch и отключение инструментов не трогаем.

**Файлы:** `scripts/_impulse_loss_anatomy.py`,
`scripts/_impulse_price_audit.py` (оба новые, research)

### docs(impulse): фактическая комиссия 0.128% за круг вместо 0.110%

Наблюдение, торговая логика не менялась. Канон обосновывал тейк 0.45%
как «~4× комиссии» при VIP 0 RT 0.110%. Замер по `execFee` показал
**0.0642% на сторону = 0.128% за круг**, то есть тейк это 3.5×, а не 4×.
Порог TP/SL оставлен прежним по решению пользователя: выборки не хватает.

Заодно исправлена методика атрибуции. `closed_pnl.orderId` — это ID
**закрывающего** ордера, а закрытия по биржевому SL/TP Bybit создаёт сам,
без префикса `impulse_`. Фильтр только по префиксу занижал выборку вдвое
(38 сделок вместо 80) и убыток втрое ($199.81 вместо $637.87). Теперь
закрытия матчатся вторым проходом по (symbol, время) из локальной SQLite,
а скрипт печатает проверку согласованности (API = БД = филлы входа = филлы
выхода).

**Артефакт:** `scripts/collect_impulse_stats.py --days 10`
**Выборка:** 80 сделок, 2026-08-21 07:39 → 2026-08-31 07:39 UTC (1.4 недели),
demo, оборот $936 109, 160 филлов, проверка согласованности пройдена.

**Метрики:**
- Net PnL −$637.87, WR 28.7% (23/80), средний win $12.87 / loss $16.38, R:R 0.79
- Комиссии $600.64 = **94% убытка**; PnL без комиссий −$37.23 (−0.008% на сделку)
- Средний ход цены −0.0112% при пороге безубытка +0.128% → разрыв 0.14 п.п.
- Выходы: биржевой SL/TP 42 (SL 30 / TP 12, медиана 16с), scratch 38 (медиана 92с)
- До целей канона доходит 35%: TP 9 (11%), SL 19 (24%), обрезано между 52 (65%)
- p(WR≠50%)=0.0002, t(PnL≠0)=−4.49 p<0.0001 — значимо, но 80<100 сделок
  и 1.4<2 недели, поэтому по `sample-size.mdc` вердикт «мало данных»:
  пороги, scratch и отключение инструментов не трогаем.

Проверенная и отклонённая гипотеза: вход лимиткой post-only вместо Market
сэкономил бы 0.044% на сделку ($207 за период), убыток стал бы −$431 —
стратегия осталась бы убыточной, эффекта не хватает.

**Файлы:** `STRATEGY_RATIONALE_IMPULSE.md`, `src/impulse_bot/settings.py`
(комментарий), `scripts/collect_impulse_stats.py` (новый)

## 2026-08-24

### change(impulse): в Telegram только вход и выход

Убран стартовый «impulse старт demo=…». Логи цикла в чат не шли — спамил
рестарт. Порог, риск и сессия не менялись.

**Файлы:** `src/impulse_bot/app/main.py`

## 2026-08-21

### change(impulse): риск от виртуальных $1000, не от живого демо

Решение пользователя. Риск 1.5% считался от живого счёта (~$47 600 → $714,
лот до $280k, биржа обрезала по maxMktOrderQty). Теперь капитал —
`min(живой, VIRTUAL_CAPITAL)`, по умолчанию $1000 → риск $15. Сигнал и
SL/TP не менялись. Открытых позиций не было.

**Файлы:** `src/impulse_bot/settings.py`, `src/impulse_bot/app/main.py`,
`tests/test_impulse_bot.py`, `docker-compose.yml`, `.env.example`

## 2026-08-19

### fix(impulse): обрезать Market qty до maxMktOrderQty
`eae6961`

Вчера 22 входа (лента+кластер ок) отклонены Bybit 10001: расчётный лот
(1.5% equity / 0.25% SL ≈ $274k нотионал) больше лимита контракта.
Сигнал не меняем. Обрезаем до `lotSizeFilter.maxMktOrderQty`
(офдок https://bybit-exchange.github.io/docs/v5/market/instrument),
не `maxOrderQty` — тот для лимиток. Риск в $ на дешёвых альтах станет
меньше потолка 1.5%.

**Файлы:** `src/impulse_bot/signals.py`, `client.py`, `app/main.py`,
`tests/test_impulse_bot.py`, `STRATEGY_RATIONALE_IMPULSE.md`

## 2026-08-18

### feat(impulse): Telegram вход/выход на SCALP_TELEGRAM_*
`16024ca`

Исходящий sendMessage без поллинга (тот же токен, что у scalp). Старт,
вход, выход (SL/TP/scratch/broker_flat). Торговая логика не менялась.
https://core.telegram.org/bots/api#sendmessage

**Файлы:** `src/impulse_bot/telegram.py`, `app/main.py`, `settings.py`,
`docker-compose.yml`, `tests/test_impulse_bot.py`

### chore(impulse): heartbeat цикла в лог
`c43ec76`

Без удара контейнер писал только строку старта. Добавлен `цикл open/sess/uni`.
Торговая логика не менялась.

**Файлы:** `src/impulse_bot/app/main.py`

### feat(impulse): автомат удара на perp-альтах (лента+кластер)
`398dcd0`

Пользователь: полный автомат на Bybit perp-альтах из скрина форумов.
Не опираться на P&L scalp и старый замер «удар $30k» как на причину
не делать. Параметры — посты, не тюнинг.

**Правила.** Bitcointalk 5577812: не BTC/ETH/SOL, оборот $100k–$15M,
удар ≥$30k / ~15с и ход ≥0.2%. CScalp: вход только если лента и кластер
подтверждают сторону. FF 1014708: лондон 07–16 UTC, тейк 0.45% ≫ VIP 0
RT 0.110%. Плечо 10×, риск 1.5% equity, scratch 90с. Market + SL/TP на
ордере. Чужие позиции на общем демо не трогает.

**API.** tickers / recent-trade / create-order — Bybit v5
https://bybit-exchange.github.io/docs/v5/market/recent-trade
https://bybit-exchange.github.io/docs/v5/order/create-order

**Файлы:** `src/impulse_bot/`, `Dockerfile.impulse-bot`, `docker-compose.yml`,
`tests/test_impulse_bot.py`, `STRATEGY_RATIONALE_IMPULSE.md`
