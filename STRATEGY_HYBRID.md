# H-HYBRID — канон гипотезы (ETH-контур)

**Статус:** гипотеза в исследовании. **Не стратегия.** Кода нет, торговать по
ней нельзя, пока не пройдены гейты из §7.

**Приоритет:** решение пользователя 2026-08-20 — H-HYBRID является главным
направлением исследования. Старые стратегии скальпа (`sweep_fade*`,
`density_*`) как самостоятельные источники края больше не приоритет; скальп
рассматривается как **генератор триггеров** для контура (§3).

Единый источник правды по терминологии и цифрам. Первичные наблюдения —
`BUILDLOG_SCALP.md` (записи 2026-08-19 / 2026-08-20) и `BUILDLOG_HORIZON.md`.

---

## 1. Терминология (использовать только эти слова)

| Термин | Значение |
|---|---|
| **ETH-контур** / **H-HYBRID** | Вся связка целиком. Единица учёта и анализа |
| **Ядро** (core) | Трендовая позиция, которая держит инвентарь. Сейчас `swing-bot`, SMA20/50 4h, 15% equity, long/flat, **без своего SL** |
| **Якорь** (anchor) | Цена входа текущего ядра. Ключевая величина: знак P&L контура определяется положением фиксации относительно якоря |
| **Тактика** (trigger) | Скальп-сигнал, который спускает курок фиксации. Любая страта, любой выход |
| **Лок** (lock) | Фактическая цена, по которой биржа закрыла общий лот |
| **Перезаход** (re-entry) | Немедленный вход ядра обратно, пока трендовое правило ещё говорит long |

Как **не** называть: «прибыльная серия скальпа», «`density_break` работает»,
«скальп победил комиссию». Это разные слои, их смешение и было исходной
ошибкой.

## 2. Что наблюдалось (факты, не интерпретация)

Bybit linear **one-way**: один лот на символ. Тактика и ядро физически делят
одну позицию. Тактический SL/TP (`tpslMode=Full`) и дискреционный выход
(`close_market(pos.size)`) действуют на **весь** лот.

Шесть тикетов, 2026-08-18 15:41 → 2026-08-20 07:17 UTC, ETHUSDT:

| # | страта-триггер | тип выхода | лок | якорь ядра | pnl в БД | своя геометрия |
|---|---|---|---|---|---|---|
| 4218 | `sweep_fade_canon` | вход-неттинг | 1917.02 | 1902.27 | +$23.69 | +$0.73 |
| 4220 | `sweep_fade_canon` | `flow_exit` / `pos.size` | 1918.67 | 1902.27 | +$40.61 | +$15.73 |
| 4266 | `density_break` | Full-SL | 2097.02 | 1918.20 | +$618.83 | −$10.02 |
| 4267 | `density_break` | Full-SL | 2090.45 | 2092.10 | −$23.41 | −$10.11 |
| 4276 | `density_break` | Full-TP | 2120.91 | 2089.99 | +$130.76 | +$34.95 |
| 4288 | `sweep_fade` | Full-SL | 2251.99 | 2126.86 | +$393.95 | −$9.70 |

Ключевое: **контур наследует любая страта**, попавшая на символ с ядром. Три
разные страты, четыре разных типа выхода. Атрибут эффекта — пара
(символ, есть ли лот ядра), не имя стратегии.

## 3. Формулировка гипотезы

> One-way + трендовое ядро без своего стопа + ордерфлоу-триггер фиксации +
> немедленный перезаход печатает прибыль, **пока лок остаётся выше якоря
> текущего ядра** (для long).

`P&L_контура ≈ (лок − якорь) × лот_ядра + своя_геометрия_тактики
− 0.055% × Σ(все ноги)`

Матожидание даёт **ядро** (направление), тактика влияет только на качество
момента фиксации, комиссия растёт линейно с числом фиксаций.

## 4. Что уже подтверждено

- **Механизм.** 6 событий за ~40 ч на одном символе. Полный набор триггеров:
  вход-неттинг, `flow_exit`/`pos.size`, Full-SL, Full-TP. Воспроизводится
  надёжно, это не шум.
- **Асимметрия по якорю.** Зелёный только при локе выше якоря. Контроль:
  #4267 (якорь 2092, лок 2090) −$23; #4219 BTC (якорь ядра 64407.6, лок
  ниже) −$38.51. Растущий рынок сам по себе контур не кормит.
- **Источник плюса — ход ядра.** У тактики своя геометрия от −$10 до +$35;
  в TG уходили суммы до +$619.

## 5. Что уже опровергнуто (не повторять)

- **«Это край скальпа».** Своя геометрия тактики около нуля или в минусе в
  5 из 6 тикетов.
- **«Это решение проблемы комиссий».** Тариф не менялся (taker 0.055%,
  RT 0.110%). Доля комиссии падает лишь потому, что в знаменателе ход ядра
  0.8–9.3%. Метрика `fees/net` на контуре обманчива.
- **«Перезаход спасает раннер» (P4).** Бенчмарк на всей траектории
  18.08–20.08, ETH 1902 → 2253 (+18.5%):

| | контур (факт) | просто держать 3.59 @ 1902.27 |
|---|---|---|
| gross | +$1219.82 | +$1259.50 |
| ноги комиссии | **17** | 1 |
| комиссия | −$59.56 | −$3.76 |
| **net** | **+$1160.26** | **+$1256.76** |

  **Контур отстал на −$96.51 (−7.7%).** Две утечки: 17 ног вместо одной и
  падение лота ядра 3.59 → 3.11 (−13.4%), потому что 15% equity делится на
  растущую цену. Пары «лок → перезаход»: −0.024% / −0.235% / −0.022% /
  **+0.281%** / **+0.062%** — преимущества входа нет.

## 6. Ограничения текущих данных

- **Один режим.** 19.08 — шорт-сквиз: BTC впервые с 2 июня выше $70k, ETH
  выше $2300, ликвидации шортов ~$2.7 млрд/24ч. Лучший фон для холда,
  худший для фиксаций. Обобщать нельзя.
- **Один символ, n=6.** Порог `sample-size.mdc` (≥100, ≥2 недели, оба
  режима) не пройден и близко.
- **Учёт сломан.** Одна биржевая сделка попадает в две БД: скальп (verified
  `closedPnl` всего лота) и `swing` (`broker_flat` по `last_price`).
  Двойной счёт до $1212 при реальном fill $620.65.
- **Ядро слабое.** SMA20/50 4h не прошёл IS/OOS
  (`scripts/scalp_swing_research.py`). `daytrend-bot` (SMA200 daily,
  прошёл 5 лет BTC) всю ралли стоял флэт — два наших трендовых правила
  разошлись.
- **Карантин данных.** Строки `symbol IN ('ETHUSDT','BTCUSDT') AND
  ts_open >= 2026-08-18T11:19Z` (старт `swing-bot`) непригодны для
  per-strategy выводов: после старта ядра ETH+BTC n=8 net **+$1158.89**
  против прочих символов n=1 net **−$13.62**.

## 7. План исследования (по порядку; артефакт на каждом шаге)

**Шаг 0 — измеритель. СДЕЛАНО 2026-08-20.** `scripts/hybrid_contour_pnl.py`,
детали в §10.

**Шаг 1 — бенчмарк во всех режимах.** Контур vs «держать ядро» на истории:
тренд вверх, тренд вниз, чоп. Гипотеза сейчас проигрывает в тренде; вопрос,
выигрывает ли в чопе и в дауне (там ранняя фиксация должна помогать).

**Шаг 2 — ценность триггера.** Сравнить ордерфлоу-триггер против baseline:
фиксация по ATR-трейлингу, по фиксированному %, в случайные моменты. Если
скальп-триггер не лучше случайного — тактика бесполезна, остаётся чистое
ядро, и гипотеза закрывается.

**Шаг 3 — порог фиксации.** Правило «фиксировать только если
(лок − якорь) ≥ k × RT-издержки». Найти k на истории, проверить OOS. Это
прямое лечение утечки на 17 ногах.

**Шаг 4 — выбор ядра.** SMA20/50 4h против SMA200 daily против других
трендовых правил, уже имеющих подтверждение. Ядро — источник матожидания,
его качество важнее триггера.

**Шаг 5 — сайзинг.** Убрать падение лота в тренде (15% equity / растущая
цена). Проверить фиксированный лот или сайзинг от волатильности.

**Шаг 6 — только после шагов 0–5:** явная реализация контура одним ботом с
собственным учётом и раздельным UID.

## 8. Гейты приёмки (до них — не стратегия)

1. Контур обгоняет «держать ядро» net-of-fees на выборке ≥100 фиксаций,
   ≥2 недели, минимум два режима (`sample-size.mdc`).
2. Триггер обгоняет случайную фиксацию и ATR-трейлинг (шаг 2).
3. Знак результата совпадает IS и OOS.
4. Учёт сходится с биржевой выпиской 1:1 (`stats-collection.mdc`).

## 9. Запреты

- Не заявлять край и не включать live-торговлю контура до §8.
- Не тюнить окна SMA, пороги RSI/ADX/стены под эти 6 тикетов
  (`no-data-fitting.mdc`).
- Не считать тикеты контура в статистику стратегий скальпа.
- Не читать `fees/net` контура как улучшение тарифа.
- Не выключать `scalp-bot` и `swing-bot` без обсуждения: они сейчас
  единственный живой источник наблюдений контура.

---

## 10. Измеритель контура (шаг 0, готов 2026-08-20)

`scripts/hybrid_contour_pnl.py` — read-only, локальные БД не читает вообще.
Двойной счёт брался именно из сложения двух БД, поэтому единственный
источник — биржа (`stats-collection.mdc`).

**Что откуда берётся.**

| Величина | Источник | Почему так |
|---|---|---|
| Реализации, net | `/v5/position/closed-pnl` → `closedPnl` | ground truth, комиссии уже внутри |
| **Якорь** смешанной позиции | там же → `avgEntryPrice` | средний вход закрытого объёма. Реконструкция из двух БД не нужна |
| Лок | там же → `avgExitPrice` | |
| Ноги, комиссии, maker/taker | `/v5/execution/list` → `execFee`, `isMaker`, `feeRate` | прямой замер «налога на ноги» |
| Кто инициировал ногу | `orderLinkId`: `scalp_` / `swing_` / `daytrend_`; пусто + `stopOrderType` = биржевой брекет | атрибуция на shared account |
| Остаток, unrealized | `/v5/position/list` | |

**Методологическая находка: `Funding` — отдельный `execType`.** В истории
исполнений funding-начисление приходит записью, где `execQty` равен размеру
позиции, а `execFee` — сам платёж
(<https://bybit-exchange.github.io/docs/v5/enum#exectype>). Если не отделить
эти записи, книга рассыпается: до фильтра остаток по ETH считался 20.33
против биржевых 3.11. После фильтра книга сходится с биржей точно, а
расхождение gross упало до $2.01 на ETH и $0.12 на BTC. Это выполняет
гейт §8.4.

Функционально funding важен и по существу: холд платит его на полный лот всё
время, контур — только пока позиция открыта. Поэтому бенчмарк вычитает
funding из обеих ветвей (для холда ставка восстанавливается из фактических
начислений как `execFee / execValue`).

**Первый замер, окно 2026-08-17 08:09 → 2026-08-20 08:09 UTC.** Оба остатка
открыты, выходная комиссия не учтена ни у контура, ни у холда.

| | ETHUSDT | BTCUSDT |
|---|---|---|
| реализовано (net) | +$1108.11 | −$43.23 |
| unrealized остатка | +$28.64 | +$653.82 |
| funding | −$2.71 | −$1.92 |
| **контур итого** | **+$1134.05** | **+$608.67** |
| ноги / комиссия | 25 / $74.20 | 8 / $21.06 |
| холд ядра | 3.59 @ 1902.27 → **+$1286.68** | 0.106 @ 64407.90 → **+$626.97** |
| **Δ контур − холд** | **−$152.63** | **−$18.30** |

Итого контур отстаёт на **−$170.93**. Вывод бенчмарка от 2026-08-20
(§5, P4) подтверждён на независимом источнике: отставание не артефакт
методики, а факт. Комиссия ног $95.26 против ~$7.51 у холда — основная
утечка.

**Кто на самом деле закрывает.** Из 10 реализаций ETH восемь сделаны
биржевым брекетом (`StopLoss` × 7, `TakeProfit` × 1) и только две — активным
решением скальпа (`scalp_ETHUSDT_*` вход-неттинг, `scalp_flow_exit`). То
есть фиксацию в контуре задаёт **геометрия брекета скальпа**, а не его
ордерфлоу-логика. Это прямо задаёт форму шага 2: сравнивать надо не
«ордерфлоу против ATR», а «случайный по геометрии стоп против осмысленного
правила выхода».

**Запуск.**

```bash
ssh root@204.168.149.140 \
  "docker exec -i fx-pro-bot-scalp-bot-1 python3 - --days 3 --snapshot" \
  < scripts/hybrid_contour_pnl.py
```

`--snapshot` дописывает строку в `/data/hybrid_contour.csv` (том
`scalp_bot_data`) — накопитель для наблюдения тенденции. Окно API ограничено
7 днями на запрос, скрипт нарезает `--days` на чанки сам.

**Наблюдение поставлено на cron (VPS, UTC):** каждые 4 часа `--days 7
--snapshot`, лог `/var/log/hybrid-contour-cron.log`. Логику ботов не трогает,
контейнеры не рестартует.

```cron
5 */4 * * * cd /root/fx-pro-bot && /usr/bin/docker exec -i \
  fx-pro-bot-scalp-bot-1 python3 - --days 7 --snapshot \
  < scripts/hybrid_contour_pnl.py >> /var/log/hybrid-contour-cron.log 2>&1
```

Что снапшот даёт, чего не даёт API постфактум: история реализаций и ног
доступна 7 дней назад в любой момент, поэтому ряд восстановим и без cron.
Невосстановима напрямую только **траектория unrealized** по открытому
остатку — за ней и нужен регулярный снимок.

**Читать снапшоты с осторожностью (см. также §11).** На окне 7 дней BTC показал `контур
ВПЕРЕДИ +$4.52`, но там же сработало предупреждение сверки (книга 0.158 vs
биржа 0.106): в окно попали реализации от позиции, открытой раньше начала
окна, и бенчмарк холда считается от неполного якоря. Такие строки — не
результат, а сигнал расширить окно. Валидны только замеры, где книга сходится
с биржей — в CSV это `book_aligned=1`; при анализе ряда фильтровать по нему
обязательно (строки с `0` читать нельзя, см. §11 п.4).

---

## 11. Метрики: что они закрыли (2026-08-20)

До этого мы видели только итог `contour_total` и не могли ответить, из чего он
складывается. Добавлены метрики ниже; арифметика закреплена тестами
`tests/test_hybrid_contour.py`. Значения — окно 17.08→20.08 UTC, ETHUSDT,
книга сошлась с биржей (расх. $2.71).

**Декомпозиция лота на ядро и тактику.** Биржа держит один лот, поэтому
вопрос «сколько в P&L хода ядра» из API не читается. Состав лота
восстанавливается по `orderLinkId` входов, закрываемый объём делится
**pro-rata** (не FIFO: биржа считает avg-cost, порядок лотов не наблюдаем).
Результат: **ядро $889.87 (71%), тактика $370.14 (29%)**.

Важное чтение: 29% — это **не край тактики**. Это ход тренда, пришедшийся на
объём, который тактика внесла в общий лот. Своя геометрия тактики по-прежнему
около нуля (§2). Метрика отвечает на «чей объём ехал», не на «чей сигнал
работал».

**Кто закрывает ядро.** Самое сильное наблюдение: **8 из 8 закрытий ядра —
принудительные**, собственным решением horizon не закрыто ни одного. То есть
в контуре трендовое правило SMA20/50 управляет **только входами**; выходы
целиком задаёт брекет скальпа. «Ядро SMA20/50» внутри контура — это не то
правило, что проверялось research-ом, у него ампутирован выход. Это меняет
шаг 4: выбирать надо не «ядро», а пару (правило входа, правило выхода).

**Асимметрия по якорю.** 8 из 11 фиксаций выше якоря (73%), медиана
**+0.674%**, разброс −0.303%…+6.172%. На фиксации выше якоря приходится
$1245.35 при итоге $1168.97 (107%) — то есть минусовые фиксации отнимают.
Центральное утверждение §3 подтверждается количественно, но n=11.

**Перезаход почти бесплатен.** 6 перезаходов, суммарно **−$7.23**, паузы
0–2 минуты, ядро в рынке **100%** времени с первого входа. Ранняя догадка,
что утечка в простое вне рынка, **неверна** — простоя нет.

**Sizing drag** −13.9% (первый вход ядра 3.59 → последний 3.09). **Лот смешан
с тактикой 15%** времени — это и есть окно экспозиции к контаминации.

**Что из этого следует про источник отставания.** Перезаходы (−$7) и drag
(единицы долларов) отставание −$157 не объясняют. Остаются два фактора:
комиссия лишних ног (−$70 против −$3.76 у холда) и работа меньшим средним
лотом на большом ходе. Значит шаг 3 (порог фиксации) и шаг 5 (сайзинг) важнее
шага 2.

**Риск, которого не видно в итоге.** Подушка остатка **+0.25%** (mark 2288.90
против якоря 2283.08) на нотионале $7073; худший ход от последнего входа
+0.19%. MAE/MFE считается по 15m свечам от последнего входа ядра.

**Дефекты метрик, найденные и исправленные сразу** (иначе они бы врали тихо):

1. *Знаменатель времени.* «Ядро в рынке 62%» было артефактом: окно
   начиналось за сутки до появления ядра. Считается от первого входа ядра →
   100%.
2. *Старт книги.* При плавающем окне первой ногой может оказаться закрытие
   позиции, открытой раньше. Реплей с нуля открывал фантомный short: сверка
   разошлась на $430, доли стали бессмысленными. Теперь старт выравнивается
   по фактической позиции биржи, лишний хвост отбрасывается (в замере
   отброшена 1 нога).
3. *Схема накопителя.* Заголовок `/data/hybrid_contour.csv` писался один раз,
   а состав полей вырос вместе с метриками: cron дописывал бы 28 значений под
   17 колонок и все дни наблюдения превратились бы в сдвинутый мусор. Схема
   зафиксирована явно, при её смене старый файл откладывается в `*.bak`.
   Поймано до первого cron-прогона, накопитель не пострадал.
4. *Достоверность декомпозиции по строкам.* Выравнивание книги удаётся не
   всегда: по **BTCUSDT** в окне 7 дней книга не сходится (расх. $+23.37,
   остаток 0.1580 против биржевых 0.1060) — доли ядро/тактика там читать
   нельзя. В снапшот добавлены `book_aligned` / `book_skipped`, чтобы такие
   строки отбрасывались при анализе, а не усреднялись с корректными.

**Слепые зоны, которые метриками НЕ закрываются** — нужны другие данные:
поведение в дауне и в чопе (нужно время), ценность триггера против
альтернатив (нужен counterfactual, шаг 2), устойчивость на других символах
(§12).

---

## 12. Вселенная: можно ли уйти с эфира (2026-08-20)

**Где вообще возможен контур.** Ядро прибито к двум символам:
`SWING_SYMBOLS` и `DAYTREND_SYMBOLS` = `BTCUSDT,ETHUSDT` (docker-compose).
Скальп выбирает вселенную сам (RVOL-селектор, до 15 монет). Контур возникает
только на пересечении, поэтому расширять надо **вселенную ядра** — на
стороне скальпа делать ничего не нужно.

**Артефакт:** `scripts/hybrid_universe_scan.py`, вывод
`data/hybrid_universe_scan_out.txt`. Протокол приёмки **не переопределялся**:
функции `sma_cross` / `ok` импортируются из `scalp_swing_research.py`, гейт
тот же (n≥30, средний>0, знак совпал IS/OOS, медиана удержания 1–10 суток),
издержка та же (VIP 0 taker RT 0.110%). Порог ликвидности $100M — тот же,
что у авто-селектора скальпа. Так исключается подбор критерия под желаемый
список монет (`no-data-fitting.mdc`).

**Скан 13 символов с достаточной историей, 730 дней, 4h SMA 20/50 long/flat.**
Колонка «значим» — накрывает ли 95% CI среднего нуль.

| символ | n | ср% | **мед%** | итог% | WR | IS/OOS | значим | ER | NATR% | fixcost | вердикт |
|---|---|---|---|---|---|---|---|---|---|---|---|
| HYPEUSDT | 42 | +3.095 | −2.205 | **+129.8** | 43% | **ДА** | CI∋0 | 0.027 | 3.42 | 0.09 | ПРИНЯТ* |
| XRPUSDT | 50 | +5.927 | −0.630 | +246.3 | 40% | нет | CI∋0 | 0.006 | 1.93 | 0.16 | IS/OOS |
| 1000PEPEUSDT | 48 | +2.473 | −1.116 | +102.4 | 42% | нет | CI∋0 | 0.007 | 3.09 | 0.10 | IS/OOS |
| ZECUSDT | 47 | +5.419 | −4.816 | +59.6 | 36% | нет | CI∋0 | 0.029 | 3.30 | 0.09 | IS/OOS |
| DOGEUSDT | 52 | +1.612 | −1.421 | +38.9 | 35% | нет | CI∋0 | 0.002 | 2.43 | 0.13 | IS/OOS |
| BTCUSDT | 48 | +0.559 | −0.332 | +22.8 | 46% | нет | CI∋0 | 0.005 | 1.25 | 0.26 | IS/OOS |
| SOLUSDT | 49 | +0.244 | −0.390 | −1.1 | 43% | нет | CI∋0 | 0.007 | 2.25 | 0.14 | IS/OOS |
| **ETHUSDT** | 50 | +0.171 | −1.911 | **−14.6** | 42% | нет | CI∋0 | 0.003 | 1.93 | 0.18 | IS/OOS |
| ADAUSDT | 51 | +0.585 | −2.950 | −39.4 | 22% | нет | CI∋0 | 0.004 | 2.44 | 0.13 | IS/OOS |

**Главный результат скана: значимого края нет ни на одной монете.** 95% CI
среднего накрывает нуль у всех 13 символов, а **медиана сделки отрицательна
везде** — даже там, где итог +246% (XRP). Правило ядра живёт редкими крупными
плюсами на фоне постоянных мелких минусов; при WR 22–46% это распределение, а
не край. Расширение вселенной **не решает** проблему контура.

**Главный вывод против зацикливания на эфире: ETH — худший из мейджоров.**
Правило ядра на нём за 2 года даёт −14.6%, и вердикт полностью зависит от
окна: 365д −19.3% (n<30), **540д +25.7% ПРИНЯТ**, 730д IS/OOS не сошёлся,
900д −48.3%. Контур мы наблюдаем именно там, где само ядро нестабильно.

**HYPEUSDT — единственный кандидат.** Положительный знак на всех доступных
окнах: 365д +24.4% (n=25), 540д +193.3% (ПРИНЯТ), 900д +129.8% (ПРИНЯТ).
NATR 3.42% против 1.93% у ETH, а `fixcost` (RT / медианный ход 4h бара) 0.09
против 0.18 — фиксация контура обходится вдвое дешевле относительно
типичного хода. Отдельно удобно, что HYPE **уже** в
`SCALP_DENSITY_BREAK_PIN_SYMBOLS`, то есть скальп там торгует и контур
возник бы естественно.

**Почему это НЕ повод включать HYPE в ядро:**

1. **Незначимо.** CI среднего [−1.909; +8.100] накрывает нуль, медиана сделки
   −2.205%. «+129.8%» — это несколько удачных сделок, а не край.
2. **Знак не держится между правилами.** Прогон HYPE по всем четырём
   правилам из `scalp_swing_research.py` за 900 дней:

   | правило | n | ср% | итог% | IS/OOS | вердикт |
   |---|---|---|---|---|---|
   | 4h Turtle 20/10 | 61 | −0.131 | **−39.5** | нет | закрыт |
   | 4h Turtle 10/5 | 97 | +0.183 | **−14.4** | ДА | «принят» |
   | 4h SMA 20/50 | 42 | +3.095 | +129.8 | ДА | «принят» |
   | Daily SMA50 | 17 | +7.143 | +61.3 | ДА | n<30 |

   Если бы HYPE был «трендовой монетой», трендовые правила не расходились бы
   так: два из четырёх дают минус. Это свойство конкретного правила и окна, а
   не символа.
3. **Дефект самого гейта, видно на этой таблице.** Turtle 10/5 «принят»
   при итоге **−14.4%**: гейт смотрит на средний PnL, а компаундирование
   отрицательной медианы съедает результат. Гейт не переопределяем (он канон
   research-а), но читать его вердикт без колонок «итог» и «значим» нельзя.
4. **Один рыночный цикл.** HYPE листингован в ноябре 2024, вся история —
   преимущественно бычий рынок. Окна 730 и 900 дней дают ту же историю, то
   есть это не независимые проверки.
5. **Множественное тестирование.** Один проход из 13 символов — ровно то, что
   даёт случайность (Harvey & Liu «Backtesting»; Bailey & Lopez de Prado,
   deflated Sharpe).

**Структурное наблюдение.** ER у всех символов крайне низкий (0.002–0.039):
на 4h направленный ход мал относительно пройденного пути, то есть рынок ближе
к пиле, чем к тренду. Это объясняет, почему трендовое ядро на 4h вообще
хрупко, независимо от символа.

**Вывод по вселенной.** Менять `SWING_SYMBOLS` сейчас не на чем: ни один
символ не даёт значимого края, а единственный «прошедший» разваливается при
смене правила. Уход с эфира сам по себе контур не спасёт — проблема не в
выборе монеты, а в том, что трендовое ядро на 4h слабо везде (ER 0.002–0.039
= рынок ближе к пиле). Приоритет остаётся на шагах 3 и 5 (порог фиксации,
сайзинг) и на выходах ядра (§11: 8 из 8 закрытий принудительные).

Что ETH — худший мейджор, всё равно стоит помнить: если понадобится второй
символ для наблюдений, брать его следует не по итогу, а по значимости и
устойчивости знака между правилами.

---

## 13. Что об этом уже известно: литература (2026-08-20)

Поиск делался **на опровержение**: искали не подтверждения, а описание
механизма и предсказания о его результате. Итог: H-HYBRID **не новая
конструкция**. Это известное явление под тремя разными именами, и в двух из
трёх литература предсказывает именно то отставание, которое мы измерили.

### 13.1 Теория портфеля: rebalancing bonus / volatility pumping

Периодическая фиксация части экспозиции с возвратом к целевому весу — это
**rebalancing bonus**, он же *volatility pumping* / *volatility harvesting* /
«Shannon's demon». Классика: Fernholz & Shay; Shannon (в изложении Luenberger);
современный разбор — Dempster/Evstigneev/Schenk-Hoppé и arXiv:1508.05241
«Volatility Harvesting: Extracting Return from Randomness».

Ключевое предсказание, дословно из working paper City University of London
«Volatility Pumping and Rebalancing Returns» (2015):

> «Rebalancing will be profitable in markets which tend to mean-revert, and
> loss-making in markets which tend to trend (as assets which have
> underperformed in the past — and so are bought by the rebalancing strategy —
> tend to continue underperforming in the future, and vice versa).»

Там же: механизм чувствителен к издержкам, а вывод «ребалансировка повышает
доходность даже без mean-reversion» назван *misleading conclusion*.

**Что это значит для нас.** Контур — это ребалансировка ядра: фиксация на
росте и перезаход. Мы наблюдали его в аптренде (шорт-сквиз 19.08) и получили
отставание от холда −$157 (§10). Это **ожидаемый результат по теории**, а не
неудача реализации. Одновременно теория даёт проверяемое условие: контур
должен выигрывать в осциллирующем рынке. Шаг 1 из §7 превращается из «посмотрим»
в проверку конкретного предсказания — и это единственная линия, где у гипотезы
остаётся шанс.

### 13.2 Практика трейдинга: «trading around a core position»

Термин устоявшийся: держать долгосрочное трендовое ядро и торговать вокруг
него на откатах (CQG commentary, «Trading Around the Core Trend-Following
Position»). **Но там фиксация делается осознанно в зонах против тренда**
(sell zone → covering zone), то есть это преднамеренный mean-reversion overlay
с выбранными уровнями. У нас фиксация происходит по геометрии стопа скальпа,
а перезаход — через 0–2 минуты почти по той же цене (§11: 6 перезаходов,
−$7.23). Наш контур **не реализует** ту практику: у нас нет зон, у нас шум.

Тот же источник даёт и предупреждение, совпадающее с нашими данными: «The very
best portion of a trend will sometimes never pullback and in those instances
holding on to your core position is essential or you'll miss the trend».

Прикладная литература по exit-методам сходится на том, что частичная фиксация
деградирует трендследование (оценки порядка 10–25% потери от полного удержания
до трейлинг-стопа), а плюсы её — психологические и по варианс, не по
матожиданию. Оговорка о качестве: это блоги и обучающие материалы, не
peer-review; вес у них ниже, чем у §13.1 и §13.3.

### 13.3 Академика по издержкам: overtrading

Barber & Odean, «Trading Is Hazardous to Your Wealth» (Journal of Finance,
2000) и «The Courage of Misguided Convictions» (FAJ, 1999), 66 465
домохозяйств, 1991–1996: самый активный квинтиль заработал 11.4% годовых
против 17.9% у рынка; **отставание активного квинтиля от собственного
buy-and-hold бенчмарка — 7.04 п.п. в год, у самого пассивного — 0.25 п.п.**
Вывод авторов: разницу объясняют издержки, а не качество отбора.

Наш первый бенчмарк дал отставание контура от холда **7.7%**
(−$96.51 / $1256.76). Совпадение порядка величины — не доказательство, но
хорошая проверка на здравый смысл: мы получили ровно ту цену, которую
литература назначает за высокий оборот.

### 13.4 Инфраструктура: netting / strategy interference

Механизм «стоп одной стратегии закрывает позицию другой» — известная проблема
неттинговых счетов, не особенность Bybit. MetaTrader 5 различает
`ACCOUNT_MARGIN_MODE_RETAIL_NETTING` (одна позиция на символ) и
`RETAIL_HEDGING`; TradersPost документирует, что позиции трекаются **по
символу, а не по стратегии**. Описание симптома в отраслевом гайде совпадает
с нашим 1:1:

> «A position closes that its own program did not close, or a stop moves to a
> level that program would never have chosen… volume simply disappears, because
> an opposing order from the other program reduced a position.»

Индустриальное лечение: раздельные субаккаунты на стратегию, hedge-mode или
tag-locking позиции за владельцем. Это подтверждает шаг 6 §7: если контур
делать стратегией, он должен быть **одним агентом с одной книгой**, а не
побочным эффектом двух ботов на общем лоте.

### 13.5 Чего в литературе не нашлось

Не найдено работ, где фиксация ядра по **ордерфлоу-триггеру другой стратегии
на общем лоте** давала бы положительный результат. То есть наша конфигурация —
это известные компоненты (трендовое ядро, ребалансировка, неттинг) в
непреднамеренной сборке; отдельного изученного феномена «H-HYBRID» нет.

### 13.6 Честный итог

Литература скорее **против** гипотезы: в тренде такая схема обязана отставать,
а издержки оборота — задокументированный налог. Это согласуется с нашими
измерениями и означает, что название H-HYBRID стоит сохранить как рабочее для
нашей конкретной сборки, но не считать её открытием.

Проверяемое, что осталось: единственное условие, при котором механизм по
теории выигрывает, — осциллирующий рынок с амплитудой, перекрывающей
издержки. Плюс наше собственное наблюдение из §11, которого в литературе по
ребалансировке нет: у ядра в контуре **ампутирован выход** (8 из 8 закрытий
принудительные). Это ближе к теме exit-методов (§13.2), и именно здесь наша
конфигурация отличается от описанных.
