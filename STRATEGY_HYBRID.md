# H-HYBRID — канон гипотезы (ETH-контур)

**Статус:** гипотеза в исследовании, **основная ветка опровергнута**. Не
стратегия. Кода нет, торговать по ней нельзя, пока не пройдены гейты из §8.

Шаг 1 плана закрыт 2026-08-20 с отрицательным результатом (§14): на 730 днях
и трёх символах контур отстаёт от простого удержания ядра во всех режимах, а
при мгновенном перезаходе — отстаёт **арифметически**, на величину лишних ног.
Живой плюс на ETH был перекладыванием нереализованного хода ядра в
реализованный, не источником края. Осталась одна проверяемая линия: фиксация
с **паузой вне рынка** плюс триггер, который размещает эту паузу лучше
случайного (§14.4).

Шаг 2 закрыт 2026-08-20, тоже отрицательно (§15): на 67 живых закрытиях за 30
дней триггер выбирает моменты **не лучше случайного часа** (45–51% при планке
50%, значимости нет). Планка §14.4 требовала перевеса ≈$950 за два года —
измеренный перевес нулевой, гейт §8.2 провален.

Шаги 4-5 закрыты 2026-08-20 (§16). Замена трендового правила ядра не помогает:
ни одно из семи канонических правил не прошло гейт, а текущее `4h SMA20/50`
уступает холду по Sharpe на BTC и ETH. Единственный положительный результат всего
исследования — **сайзинг по волатильности** (§16.2): лучше Sharpe и просадка на
9 символах из 9 плюс меньше комиссий. К контуру он не относится, работает на
голом ядре, и внедрение требует отдельного одобрения.

**Приоритет:** решение пользователя 2026-08-20 — H-HYBRID является главным
направлением исследования. Старые стратегии скальпа (`sweep_fade*`,
`density_*`) как самостоятельные источники края больше не приоритет; скальп
рассматривается как **генератор триггеров** для контура (§3).

Единый источник правды по терминологии и цифрам. Первичные наблюдения —
`BUILDLOG_SCALP.md` (записи 2026-08-19 / 2026-08-20) и `BUILDLOG_HORIZON.md`.

---

## 1. Терминология (использовать только эти слова)

| Термин | Значение |
|---|---|
| **ETH-контур** / **H-HYBRID** | Вся связка целиком. Единица учёта и анализа |
| **Ядро** (core) | Трендовая позиция, которая держит инвентарь. Сейчас `swing-bot`, SMA20/50 4h, 15% equity, long/flat, **без своего SL** |
| **Якорь** (anchor) | Цена входа текущего ядра. Ключевая величина: знак P&L контура определяется положением фиксации относительно якоря |
| **Тактика** (trigger) | Скальп-сигнал, который спускает курок фиксации. Любая страта, любой выход |
| **Лок** (lock) | Фактическая цена, по которой биржа закрыла общий лот |
| **Перезаход** (re-entry) | Немедленный вход ядра обратно, пока трендовое правило ещё говорит long |

Как **не** называть: «прибыльная серия скальпа», «`density_break` работает»,
«скальп победил комиссию». Это разные слои, их смешение и было исходной
ошибкой.

## 2. Что наблюдалось (факты, не интерпретация)

Bybit linear **one-way**: один лот на символ. Тактика и ядро физически делят
одну позицию. Тактический SL/TP (`tpslMode=Full`) и дискреционный выход
(`close_market(pos.size)`) действуют на **весь** лот.

Шесть тикетов, 2026-08-18 15:41 → 2026-08-20 07:17 UTC, ETHUSDT:

| # | страта-триггер | тип выхода | лок | якорь ядра | pnl в БД | своя геометрия |
|---|---|---|---|---|---|---|
| 4218 | `sweep_fade_canon` | вход-неттинг | 1917.02 | 1902.27 | +$23.69 | +$0.73 |
| 4220 | `sweep_fade_canon` | `flow_exit` / `pos.size` | 1918.67 | 1902.27 | +$40.61 | +$15.73 |
| 4266 | `density_break` | Full-SL | 2097.02 | 1918.20 | +$618.83 | −$10.02 |
| 4267 | `density_break` | Full-SL | 2090.45 | 2092.10 | −$23.41 | −$10.11 |
| 4276 | `density_break` | Full-TP | 2120.91 | 2089.99 | +$130.76 | +$34.95 |
| 4288 | `sweep_fade` | Full-SL | 2251.99 | 2126.86 | +$393.95 | −$9.70 |

Ключевое: **контур наследует любая страта**, попавшая на символ с ядром. Три
разные страты, четыре разных типа выхода. Атрибут эффекта — пара
(символ, есть ли лот ядра), не имя стратегии.

## 3. Формулировка гипотезы

> One-way + трендовое ядро без своего стопа + ордерфлоу-триггер фиксации +
> немедленный перезаход печатает прибыль, **пока лок остаётся выше якоря
> текущего ядра** (для long).

`P&L_контура ≈ (лок − якорь) × лот_ядра + своя_геометрия_тактики
− 0.055% × Σ(все ноги)`

Матожидание даёт **ядро** (направление), тактика влияет только на качество
момента фиксации, комиссия растёт линейно с числом фиксаций.

## 4. Что уже подтверждено

- **Механизм.** 6 событий за ~40 ч на одном символе. Полный набор триггеров:
  вход-неттинг, `flow_exit`/`pos.size`, Full-SL, Full-TP. Воспроизводится
  надёжно, это не шум.
- **Асимметрия по якорю.** Зелёный только при локе выше якоря. Контроль:
  #4267 (якорь 2092, лок 2090) −$23; #4219 BTC (якорь ядра 64407.6, лок
  ниже) −$38.51. Растущий рынок сам по себе контур не кормит.
- **Источник плюса — ход ядра.** У тактики своя геометрия от −$10 до +$35;
  в TG уходили суммы до +$619.

## 5. Что уже опровергнуто (не повторять)

- **«Это край скальпа».** Своя геометрия тактики около нуля или в минусе в
  5 из 6 тикетов.
- **«Это решение проблемы комиссий».** Тариф не менялся (taker 0.055%,
  RT 0.110%). Доля комиссии падает лишь потому, что в знаменателе ход ядра
  0.8–9.3%. Метрика `fees/net` на контуре обманчива.
- **«Перезаход спасает раннер» (P4).** Бенчмарк на всей траектории
  18.08–20.08, ETH 1902 → 2253 (+18.5%):

| | контур (факт) | просто держать 3.59 @ 1902.27 |
|---|---|---|
| gross | +$1219.82 | +$1259.50 |
| ноги комиссии | **17** | 1 |
| комиссия | −$59.56 | −$3.76 |
| **net** | **+$1160.26** | **+$1256.76** |

  **Контур отстал на −$96.51 (−7.7%).** Две утечки: 17 ног вместо одной и
  падение лота ядра 3.59 → 3.11 (−13.4%), потому что 15% equity делится на
  растущую цену. Пары «лок → перезаход»: −0.024% / −0.235% / −0.022% /
  **+0.281%** / **+0.062%** — преимущества входа нет.

## 6. Ограничения текущих данных

- **Один режим.** 19.08 — шорт-сквиз: BTC впервые с 2 июня выше $70k, ETH
  выше $2300, ликвидации шортов ~$2.7 млрд/24ч. Лучший фон для холда,
  худший для фиксаций. Обобщать нельзя.
- **Один символ, n=6.** Порог `sample-size.mdc` (≥100, ≥2 недели, оба
  режима) не пройден и близко.
- **Учёт сломан.** Одна биржевая сделка попадает в две БД: скальп (verified
  `closedPnl` всего лота) и `swing` (`broker_flat` по `last_price`).
  Двойной счёт до $1212 при реальном fill $620.65.
- **Ядро слабое.** SMA20/50 4h не прошёл IS/OOS
  (`scripts/scalp_swing_research.py`). `daytrend-bot` (SMA200 daily,
  прошёл 5 лет BTC) всю ралли стоял флэт — два наших трендовых правила
  разошлись.
- **Карантин данных.** Строки `symbol IN ('ETHUSDT','BTCUSDT') AND
  ts_open >= 2026-08-18T11:19Z` (старт `swing-bot`) непригодны для
  per-strategy выводов: после старта ядра ETH+BTC n=8 net **+$1158.89**
  против прочих символов n=1 net **−$13.62**.

## 7. План исследования (по порядку; артефакт на каждом шаге)

**Шаг 0 — измеритель. СДЕЛАНО 2026-08-20.** `scripts/hybrid_contour_pnl.py`,
детали в §10.

**Шаг 1 — бенчмарк во всех режимах. СДЕЛАНО 2026-08-20, результат
отрицательный.** `scripts/hybrid_contour_sim.py`, 730 дней, 3 символа, детали
в §14. Контур отстаёт от холда во всех режимах; предсказание литературы про
чоп не подтвердилось; при мгновенном перезаходе отставание — арифметическое,
а не статистическое. Гейт §8.1 провален по существу.

**Шаг 2 — ценность триггера. СДЕЛАНО 2026-08-20, результат отрицательный.**
`scripts/hybrid_trigger_skill.py`, детали в §15. Планка из §14.4: переиграть
случайное размещение паузы на ≈$950 за два года. На истории шаг не проверить
(плотностей стакана за два года нет), поэтому источник — live-ноги: 67 закрытий
за 30 дней. Триггер попадает в моменты не лучше случайного часа (45–51%,
значимости нет), перевес нулевой, гейт §8.2 провален. Остаточная линия — пауза
плюс **чистый** ордерфлоу-триггер в нисходящем режиме (в выборке его почти нет).

**Шаг 2а — пауза вне рынка (новое, из §14.3).** Без паузы фиксация не меняет
экспозицию вообще, то есть текущая конфигурация не может выиграть ни в каком
режиме. Любая дальнейшая версия контура обязана держать паузу; её длина —
параметр, который надо валидировать, а не выбирать по лучшей ячейке сетки
(0 из 30 ячеек устойчивы по символам).

**Шаг 3 — порог фиксации.** Правило «фиксировать только если
(лок − якорь) ≥ k × RT-издержки». Найти k на истории, проверить OOS. Это
прямое лечение утечки на 17 ногах. **Смысл потерян после §15:** порог полезен
только если моменты фиксации несут информацию, а они её не несут.

**Шаг 4 — выбор ядра. СДЕЛАНО 2026-08-20, результат отрицательный.**
`scripts/hybrid_core_select.py`, детали в §16.1. Семь канонических трендовых
правил, 1460 дней, отбор на BTC/ETH/SOL и OOS на шести других символах. Гейт не
прошло ни одно; единственный кандидат отбора (`D SMA50`) OOS не подтвердился.
Текущее ядро `4h SMA20/50` уступает холду по Sharpe на BTC и ETH.

**Шаг 5 — сайзинг. СДЕЛАНО 2026-08-20, результат положительный.** Детали в
§16.2. Цель по волатильности (MOP 2012) обходит текущую долю счёта по Sharpe и
по просадке на 9 символах из 9 и попутно срезает комиссии и funding. Это
единственный положительный результат всего исследования. К контуру отношения не
имеет — улучшает голое ядро. Внедрение требует одобрения (`strategy-guard.mdc`).

**Шаг 6 — только после шагов 0–5:** явная реализация контура одним ботом с
собственным учётом и раздельным UID.

## 8. Гейты приёмки (до них — не стратегия)

1. Контур обгоняет «держать ядро» net-of-fees на выборке ≥100 фиксаций,
   ≥2 недели, минимум два режима (`sample-size.mdc`).
2. Триггер обгоняет случайную фиксацию и ATR-трейлинг (шаг 2).
3. Знак результата совпадает IS и OOS.
4. Учёт сходится с биржевой выпиской 1:1 (`stats-collection.mdc`).

## 9. Запреты

- Не заявлять край и не включать live-торговлю контура до §8.
- Не тюнить окна SMA, пороги RSI/ADX/стены под эти 6 тикетов
  (`no-data-fitting.mdc`).
- Не считать тикеты контура в статистику стратегий скальпа.
- Не читать `fees/net` контура как улучшение тарифа.
- Не выключать `scalp-bot` и `swing-bot` без обсуждения: они сейчас
  единственный живой источник наблюдений контура.

---

## 10. Измеритель контура (шаг 0, готов 2026-08-20)

`scripts/hybrid_contour_pnl.py` — read-only, локальные БД не читает вообще.
Двойной счёт брался именно из сложения двух БД, поэтому единственный
источник — биржа (`stats-collection.mdc`).

**Что откуда берётся.**

| Величина | Источник | Почему так |
|---|---|---|
| Реализации, net | `/v5/position/closed-pnl` → `closedPnl` | ground truth, комиссии уже внутри |
| **Якорь** смешанной позиции | там же → `avgEntryPrice` | средний вход закрытого объёма. Реконструкция из двух БД не нужна |
| Лок | там же → `avgExitPrice` | |
| Ноги, комиссии, maker/taker | `/v5/execution/list` → `execFee`, `isMaker`, `feeRate` | прямой замер «налога на ноги» |
| Кто инициировал ногу | `orderLinkId`: `scalp_` / `swing_` / `daytrend_`; пусто + `stopOrderType` = биржевой брекет | атрибуция на shared account |
| Остаток, unrealized | `/v5/position/list` | |

**Методологическая находка: `Funding` — отдельный `execType`.** В истории
исполнений funding-начисление приходит записью, где `execQty` равен размеру
позиции, а `execFee` — сам платёж
(<https://bybit-exchange.github.io/docs/v5/enum#exectype>). Если не отделить
эти записи, книга рассыпается: до фильтра остаток по ETH считался 20.33
против биржевых 3.11. После фильтра книга сходится с биржей точно, а
расхождение gross упало до $2.01 на ETH и $0.12 на BTC. Это выполняет
гейт §8.4.

Функционально funding важен и по существу: холд платит его на полный лот всё
время, контур — только пока позиция открыта. Поэтому бенчмарк вычитает
funding из обеих ветвей (для холда ставка восстанавливается из фактических
начислений как `execFee / execValue`).

**Первый замер, окно 2026-08-17 08:09 → 2026-08-20 08:09 UTC.** Оба остатка
открыты, выходная комиссия не учтена ни у контура, ни у холда.

| | ETHUSDT | BTCUSDT |
|---|---|---|
| реализовано (net) | +$1108.11 | −$43.23 |
| unrealized остатка | +$28.64 | +$653.82 |
| funding | −$2.71 | −$1.92 |
| **контур итого** | **+$1134.05** | **+$608.67** |
| ноги / комиссия | 25 / $74.20 | 8 / $21.06 |
| холд ядра | 3.59 @ 1902.27 → **+$1286.68** | 0.106 @ 64407.90 → **+$626.97** |
| **Δ контур − холд** | **−$152.63** | **−$18.30** |

Итого контур отстаёт на **−$170.93**. Вывод бенчмарка от 2026-08-20
(§5, P4) подтверждён на независимом источнике: отставание не артефакт
методики, а факт. Комиссия ног $95.26 против ~$7.51 у холда — основная
утечка.

**Кто на самом деле закрывает.** Из 10 реализаций ETH восемь сделаны
биржевым брекетом (`StopLoss` × 7, `TakeProfit` × 1) и только две — активным
решением скальпа (`scalp_ETHUSDT_*` вход-неттинг, `scalp_flow_exit`). То
есть фиксацию в контуре задаёт **геометрия брекета скальпа**, а не его
ордерфлоу-логика. Это прямо задаёт форму шага 2: сравнивать надо не
«ордерфлоу против ATR», а «случайный по геометрии стоп против осмысленного
правила выхода».

**Запуск.**

```bash
ssh root@204.168.149.140 \
  "docker exec -i fx-pro-bot-scalp-bot-1 python3 - --days 3 --snapshot" \
  < scripts/hybrid_contour_pnl.py
```

`--snapshot` дописывает строку в `/data/hybrid_contour.csv` (том
`scalp_bot_data`) — накопитель для наблюдения тенденции. Окно API ограничено
7 днями на запрос, скрипт нарезает `--days` на чанки сам.

**Наблюдение поставлено на cron (VPS, UTC):** каждые 4 часа `--days 7
--snapshot`, лог `/var/log/hybrid-contour-cron.log`. Логику ботов не трогает,
контейнеры не рестартует.

```cron
5 */4 * * * cd /root/fx-pro-bot && /usr/bin/docker exec -i \
  fx-pro-bot-scalp-bot-1 python3 - --days 7 --snapshot \
  < scripts/hybrid_contour_pnl.py >> /var/log/hybrid-contour-cron.log 2>&1
```

Что снапшот даёт, чего не даёт API постфактум: история реализаций и ног
доступна 7 дней назад в любой момент, поэтому ряд восстановим и без cron.
Невосстановима напрямую только **траектория unrealized** по открытому
остатку — за ней и нужен регулярный снимок.

**Читать снапшоты с осторожностью (см. также §11).** На окне 7 дней BTC показал `контур
ВПЕРЕДИ +$4.52`, но там же сработало предупреждение сверки (книга 0.158 vs
биржа 0.106): в окно попали реализации от позиции, открытой раньше начала
окна, и бенчмарк холда считается от неполного якоря. Такие строки — не
результат, а сигнал расширить окно. Валидны только замеры, где книга сходится
с биржей — в CSV это `book_aligned=1` **и** малое `|book_gross_delta|`;
фильтровать по обоим полям обязательно (см. §11 п.4). После выравнивания
старта книги это предупреждение по BTC не воспроизводится.

---

## 11. Метрики: что они закрыли (2026-08-20)

До этого мы видели только итог `contour_total` и не могли ответить, из чего он
складывается. Добавлены метрики ниже; арифметика закреплена тестами
`tests/test_hybrid_contour.py`. Значения — окно 17.08→20.08 UTC, ETHUSDT,
книга сошлась с биржей (расх. $2.71).

**Декомпозиция лота на ядро и тактику.** Биржа держит один лот, поэтому
вопрос «сколько в P&L хода ядра» из API не читается. Состав лота
восстанавливается по `orderLinkId` входов, закрываемый объём делится
**pro-rata** (не FIFO: биржа считает avg-cost, порядок лотов не наблюдаем).
Результат: **ядро $889.87 (71%), тактика $370.14 (29%)**.

Важное чтение: 29% — это **не край тактики**. Это ход тренда, пришедшийся на
объём, который тактика внесла в общий лот. Своя геометрия тактики по-прежнему
около нуля (§2). Метрика отвечает на «чей объём ехал», не на «чей сигнал
работал».

**Кто закрывает ядро.** Самое сильное наблюдение: **8 из 8 закрытий ядра —
принудительные**, собственным решением horizon не закрыто ни одного. То есть
в контуре трендовое правило SMA20/50 управляет **только входами**; выходы
целиком задаёт брекет скальпа. «Ядро SMA20/50» внутри контура — это не то
правило, что проверялось research-ом, у него ампутирован выход. Это меняет
шаг 4: выбирать надо не «ядро», а пару (правило входа, правило выхода).

**Асимметрия по якорю.** 8 из 11 фиксаций выше якоря (73%), медиана
**+0.674%**, разброс −0.303%…+6.172%. На фиксации выше якоря приходится
$1245.35 при итоге $1168.97 (107%) — то есть минусовые фиксации отнимают.
Центральное утверждение §3 подтверждается количественно, но n=11.

**Перезаход почти бесплатен.** 6 перезаходов, суммарно **−$7.23**, паузы
0–2 минуты, ядро в рынке **100%** времени с первого входа. Ранняя догадка,
что утечка в простое вне рынка, **неверна** — простоя нет.

**Sizing drag** −13.9% (первый вход ядра 3.59 → последний 3.09). **Лот смешан
с тактикой 15%** времени — это и есть окно экспозиции к контаминации.

**Что из этого следует про источник отставания.** Перезаходы (−$7) и drag
(единицы долларов) отставание −$157 не объясняют. Остаются два фактора:
комиссия лишних ног (−$70 против −$3.76 у холда) и работа меньшим средним
лотом на большом ходе. Значит шаг 3 (порог фиксации) и шаг 5 (сайзинг) важнее
шага 2.

**Риск, которого не видно в итоге.** Подушка остатка **+0.25%** (mark 2288.90
против якоря 2283.08) на нотионале $7073; худший ход от последнего входа
+0.19%. MAE/MFE считается по 15m свечам от последнего входа ядра.

**Дефекты метрик, найденные и исправленные сразу** (иначе они бы врали тихо):

1. *Знаменатель времени.* «Ядро в рынке 62%» было артефактом: окно
   начиналось за сутки до появления ядра. Считается от первого входа ядра →
   100%.
2. *Старт книги.* При плавающем окне первой ногой может оказаться закрытие
   позиции, открытой раньше. Реплей с нуля открывал фантомный short: сверка
   разошлась на $430, доли стали бессмысленными. Теперь старт выравнивается
   по фактической позиции биржи, лишний хвост отбрасывается (в замере
   отброшена 1 нога).
3. *Схема накопителя.* Заголовок `/data/hybrid_contour.csv` писался один раз,
   а состав полей вырос вместе с метриками: cron дописывал бы 28 значений под
   17 колонок и все дни наблюдения превратились бы в сдвинутый мусор. Схема
   зафиксирована явно, при её смене старый файл откладывается в `*.bak`.
   Поймано до первого cron-прогона, накопитель не пострадал.
4. *Достоверность строки не проверялась постфактум.* В снапшоте лежали
   только результаты, без признаков сходимости — плохую строку нельзя было
   отличить от хорошей. Добавлены три поля: `book_aligned` / `book_skipped`
   (сошёлся ли **остаток** с биржей) и `book_gross_delta` (сошёлся ли
   **реализованный gross** с API). Это независимые признаки: остаток может
   совпасть, а в окно попасть реализации от позиции, открытой раньше, —
   тогда доли ядро/тактика врут. Читать строку можно только при
   `book_aligned=1` и малом `|book_gross_delta|`.

   Замер 2026-08-20 10:18 UTC, окно 7 дней: сходится оба символа — ETH
   остаток 3.0900 @ 2283.08 при расх. gross **+$2.83**, BTC 0.1060 @
   64209.30 при **−$0.64**. Предупреждение по BTC (`расх. $+23.37`, остаток
   0.1580 против 0.1060), записанное выше в §10, относится к прогону 08:25
   UTC — **до** синхронизации выравнивания книги на VPS; после неё оно не
   воспроизводится.

**Слепые зоны, которые метриками НЕ закрываются** — нужны другие данные:
поведение в дауне и в чопе (нужно время), ценность триггера против
альтернатив (нужен counterfactual, шаг 2), устойчивость на других символах
(§12).

---

## 12. Вселенная: можно ли уйти с эфира (2026-08-20)

**Где вообще возможен контур.** Ядро прибито к двум символам:
`SWING_SYMBOLS` и `DAYTREND_SYMBOLS` = `BTCUSDT,ETHUSDT` (docker-compose).
Скальп выбирает вселенную сам (RVOL-селектор, до 15 монет). Контур возникает
только на пересечении, поэтому расширять надо **вселенную ядра** — на
стороне скальпа делать ничего не нужно.

**Артефакт:** `scripts/hybrid_universe_scan.py`, вывод
`data/hybrid_universe_scan_out.txt`. Протокол приёмки **не переопределялся**:
функции `sma_cross` / `ok` импортируются из `scalp_swing_research.py`, гейт
тот же (n≥30, средний>0, знак совпал IS/OOS, медиана удержания 1–10 суток),
издержка та же (VIP 0 taker RT 0.110%). Порог ликвидности $100M — тот же,
что у авто-селектора скальпа. Так исключается подбор критерия под желаемый
список монет (`no-data-fitting.mdc`).

**Скан 13 символов с достаточной историей, 730 дней, 4h SMA 20/50 long/flat.**
Колонка «значим» — накрывает ли 95% CI среднего нуль.

| символ | n | ср% | **мед%** | итог% | WR | IS/OOS | значим | ER | NATR% | fixcost | вердикт |
|---|---|---|---|---|---|---|---|---|---|---|---|
| HYPEUSDT | 42 | +3.095 | −2.205 | **+129.8** | 43% | **ДА** | CI∋0 | 0.027 | 3.42 | 0.09 | ПРИНЯТ* |
| XRPUSDT | 50 | +5.927 | −0.630 | +246.3 | 40% | нет | CI∋0 | 0.006 | 1.93 | 0.16 | IS/OOS |
| 1000PEPEUSDT | 48 | +2.473 | −1.116 | +102.4 | 42% | нет | CI∋0 | 0.007 | 3.09 | 0.10 | IS/OOS |
| ZECUSDT | 47 | +5.419 | −4.816 | +59.6 | 36% | нет | CI∋0 | 0.029 | 3.30 | 0.09 | IS/OOS |
| DOGEUSDT | 52 | +1.612 | −1.421 | +38.9 | 35% | нет | CI∋0 | 0.002 | 2.43 | 0.13 | IS/OOS |
| BTCUSDT | 48 | +0.559 | −0.332 | +22.8 | 46% | нет | CI∋0 | 0.005 | 1.25 | 0.26 | IS/OOS |
| SOLUSDT | 49 | +0.244 | −0.390 | −1.1 | 43% | нет | CI∋0 | 0.007 | 2.25 | 0.14 | IS/OOS |
| **ETHUSDT** | 50 | +0.171 | −1.911 | **−14.6** | 42% | нет | CI∋0 | 0.003 | 1.93 | 0.18 | IS/OOS |
| ADAUSDT | 51 | +0.585 | −2.950 | −39.4 | 22% | нет | CI∋0 | 0.004 | 2.44 | 0.13 | IS/OOS |

**Главный результат скана: значимого края нет ни на одной монете.** 95% CI
среднего накрывает нуль у всех 13 символов, а **медиана сделки отрицательна
везде** — даже там, где итог +246% (XRP). Правило ядра живёт редкими крупными
плюсами на фоне постоянных мелких минусов; при WR 22–46% это распределение, а
не край. Расширение вселенной **не решает** проблему контура.

**Главный вывод против зацикливания на эфире: ETH — худший из мейджоров.**
Правило ядра на нём за 2 года даёт −14.6%, и вердикт полностью зависит от
окна: 365д −19.3% (n<30), **540д +25.7% ПРИНЯТ**, 730д IS/OOS не сошёлся,
900д −48.3%. Контур мы наблюдаем именно там, где само ядро нестабильно.

**HYPEUSDT — единственный кандидат.** Положительный знак на всех доступных
окнах: 365д +24.4% (n=25), 540д +193.3% (ПРИНЯТ), 900д +129.8% (ПРИНЯТ).
NATR 3.42% против 1.93% у ETH, а `fixcost` (RT / медианный ход 4h бара) 0.09
против 0.18 — фиксация контура обходится вдвое дешевле относительно
типичного хода. Отдельно удобно, что HYPE **уже** в
`SCALP_DENSITY_BREAK_PIN_SYMBOLS`, то есть скальп там торгует и контур
возник бы естественно.

**Почему это НЕ повод включать HYPE в ядро:**

1. **Незначимо.** CI среднего [−1.909; +8.100] накрывает нуль, медиана сделки
   −2.205%. «+129.8%» — это несколько удачных сделок, а не край.
2. **Знак не держится между правилами.** Прогон HYPE по всем четырём
   правилам из `scalp_swing_research.py` за 900 дней:

   | правило | n | ср% | итог% | IS/OOS | вердикт |
   |---|---|---|---|---|---|
   | 4h Turtle 20/10 | 61 | −0.131 | **−39.5** | нет | закрыт |
   | 4h Turtle 10/5 | 97 | +0.183 | **−14.4** | ДА | «принят» |
   | 4h SMA 20/50 | 42 | +3.095 | +129.8 | ДА | «принят» |
   | Daily SMA50 | 17 | +7.143 | +61.3 | ДА | n<30 |

   Если бы HYPE был «трендовой монетой», трендовые правила не расходились бы
   так: два из четырёх дают минус. Это свойство конкретного правила и окна, а
   не символа.
3. **Дефект самого гейта, видно на этой таблице.** Turtle 10/5 «принят»
   при итоге **−14.4%**: гейт смотрит на средний PnL, а компаундирование
   отрицательной медианы съедает результат. Гейт не переопределяем (он канон
   research-а), но читать его вердикт без колонок «итог» и «значим» нельзя.
4. **Один рыночный цикл.** HYPE листингован в ноябре 2024, вся история —
   преимущественно бычий рынок. Окна 730 и 900 дней дают ту же историю, то
   есть это не независимые проверки.
5. **Множественное тестирование.** Один проход из 13 символов — ровно то, что
   даёт случайность (Harvey & Liu «Backtesting»; Bailey & Lopez de Prado,
   deflated Sharpe).

**Структурное наблюдение.** ER у всех символов крайне низкий (0.002–0.039):
на 4h направленный ход мал относительно пройденного пути, то есть рынок ближе
к пиле, чем к тренду. Это объясняет, почему трендовое ядро на 4h вообще
хрупко, независимо от символа.

**Вывод по вселенной.** Менять `SWING_SYMBOLS` сейчас не на чем: ни один
символ не даёт значимого края, а единственный «прошедший» разваливается при
смене правила. Уход с эфира сам по себе контур не спасёт — проблема не в
выборе монеты, а в том, что трендовое ядро на 4h слабо везде (ER 0.002–0.039
= рынок ближе к пиле). Приоритет остаётся на шагах 3 и 5 (порог фиксации,
сайзинг) и на выходах ядра (§11: 8 из 8 закрытий принудительные).

Что ETH — худший мейджор, всё равно стоит помнить: если понадобится второй
символ для наблюдений, брать его следует не по итогу, а по значимости и
устойчивости знака между правилами.

---

## 13. Что об этом уже известно: литература (2026-08-20)

Поиск делался **на опровержение**: искали не подтверждения, а описание
механизма и предсказания о его результате. Итог: H-HYBRID **не новая
конструкция**. Это известное явление под тремя разными именами, и в двух из
трёх литература предсказывает именно то отставание, которое мы измерили.

### 13.1 Теория портфеля: rebalancing bonus / volatility pumping

Периодическая фиксация части экспозиции с возвратом к целевому весу — это
**rebalancing bonus**, он же *volatility pumping* / *volatility harvesting* /
«Shannon's demon». Классика: Fernholz & Shay; Shannon (в изложении Luenberger);
современный разбор — Dempster/Evstigneev/Schenk-Hoppé и arXiv:1508.05241
«Volatility Harvesting: Extracting Return from Randomness».

Ключевое предсказание, дословно из working paper City University of London
«Volatility Pumping and Rebalancing Returns» (2015):

> «Rebalancing will be profitable in markets which tend to mean-revert, and
> loss-making in markets which tend to trend (as assets which have
> underperformed in the past — and so are bought by the rebalancing strategy —
> tend to continue underperforming in the future, and vice versa).»

Там же: механизм чувствителен к издержкам, а вывод «ребалансировка повышает
доходность даже без mean-reversion» назван *misleading conclusion*.

**Что это значит для нас.** Контур — это ребалансировка ядра: фиксация на
росте и перезаход. Мы наблюдали его в аптренде (шорт-сквиз 19.08) и получили
отставание от холда −$157 (§10). Это **ожидаемый результат по теории**, а не
неудача реализации. Одновременно теория даёт проверяемое условие: контур
должен выигрывать в осциллирующем рынке. Шаг 1 из §7 превращается из «посмотрим»
в проверку конкретного предсказания — и это единственная линия, где у гипотезы
остаётся шанс.

### 13.2 Практика трейдинга: «trading around a core position»

Термин устоявшийся: держать долгосрочное трендовое ядро и торговать вокруг
него на откатах (CQG commentary, «Trading Around the Core Trend-Following
Position»). **Но там фиксация делается осознанно в зонах против тренда**
(sell zone → covering zone), то есть это преднамеренный mean-reversion overlay
с выбранными уровнями. У нас фиксация происходит по геометрии стопа скальпа,
а перезаход — через 0–2 минуты почти по той же цене (§11: 6 перезаходов,
−$7.23). Наш контур **не реализует** ту практику: у нас нет зон, у нас шум.

Тот же источник даёт и предупреждение, совпадающее с нашими данными: «The very
best portion of a trend will sometimes never pullback and in those instances
holding on to your core position is essential or you'll miss the trend».

Прикладная литература по exit-методам сходится на том, что частичная фиксация
деградирует трендследование (оценки порядка 10–25% потери от полного удержания
до трейлинг-стопа), а плюсы её — психологические и по варианс, не по
матожиданию. Оговорка о качестве: это блоги и обучающие материалы, не
peer-review; вес у них ниже, чем у §13.1 и §13.3.

### 13.3 Академика по издержкам: overtrading

Barber & Odean, «Trading Is Hazardous to Your Wealth» (Journal of Finance,
2000) и «The Courage of Misguided Convictions» (FAJ, 1999), 66 465
домохозяйств, 1991–1996: самый активный квинтиль заработал 11.4% годовых
против 17.9% у рынка; **отставание активного квинтиля от собственного
buy-and-hold бенчмарка — 7.04 п.п. в год, у самого пассивного — 0.25 п.п.**
Вывод авторов: разницу объясняют издержки, а не качество отбора.

Наш первый бенчмарк дал отставание контура от холда **7.7%**
(−$96.51 / $1256.76). Совпадение порядка величины — не доказательство, но
хорошая проверка на здравый смысл: мы получили ровно ту цену, которую
литература назначает за высокий оборот.

### 13.4 Инфраструктура: netting / strategy interference

Механизм «стоп одной стратегии закрывает позицию другой» — известная проблема
неттинговых счетов, не особенность Bybit. MetaTrader 5 различает
`ACCOUNT_MARGIN_MODE_RETAIL_NETTING` (одна позиция на символ) и
`RETAIL_HEDGING`; TradersPost документирует, что позиции трекаются **по
символу, а не по стратегии**. Описание симптома в отраслевом гайде совпадает
с нашим 1:1:

> «A position closes that its own program did not close, or a stop moves to a
> level that program would never have chosen… volume simply disappears, because
> an opposing order from the other program reduced a position.»

Индустриальное лечение: раздельные субаккаунты на стратегию, hedge-mode или
tag-locking позиции за владельцем. Это подтверждает шаг 6 §7: если контур
делать стратегией, он должен быть **одним агентом с одной книгой**, а не
побочным эффектом двух ботов на общем лоте.

### 13.5 Чего в литературе не нашлось

Не найдено работ, где фиксация ядра по **ордерфлоу-триггеру другой стратегии
на общем лоте** давала бы положительный результат. То есть наша конфигурация —
это известные компоненты (трендовое ядро, ребалансировка, неттинг) в
непреднамеренной сборке; отдельного изученного феномена «H-HYBRID» нет.

### 13.6 Честный итог

Литература скорее **против** гипотезы: в тренде такая схема обязана отставать,
а издержки оборота — задокументированный налог. Это согласуется с нашими
измерениями и означает, что название H-HYBRID стоит сохранить как рабочее для
нашей конкретной сборки, но не считать её открытием.

Проверяемое, что осталось: единственное условие, при котором механизм по
теории выигрывает, — осциллирующий рынок с амплитудой, перекрывающей
издержки. Плюс наше собственное наблюдение из §11, которого в литературе по
ребалансировке нет: у ядра в контуре **ампутирован выход** (8 из 8 закрытий
принудительные). Это ближе к теме exit-методов (§13.2), и именно здесь наша
конфигурация отличается от описанных.

---

## 14. Шаг 1: контур против холда на истории (2026-08-20)

Артефакт: `scripts/hybrid_contour_sim.py`, вывод —
`data/hybrid_contour_sim_out.txt`, арифметика под тестами
(`tests/test_hybrid_sim.py`, 9 тестов). Окно **2024-08-20 → 2026-08-20**,
730 дней, 17 520 часовых баров и 2 190 фактических funding-начислений на
символ; ETHUSDT, BTCUSDT, SOLUSDT.

Ядро воспроизведено по коду, а не по описанию: `sma20_50_4h` из
`src/horizon_bot/signals.py`, нотионал `equity × 0.15`
(`SWING_POSITION_FRAC`, docker-compose.yml), лот = нотионал / цена, плечо 1,
сигнал на close 4h-бара, сделка на open следующего. Equity держится
постоянным ($10 000) намеренно: иначе вклады эпизодов нельзя складывать по
режимам. Просадка лота в тренде при этом сохраняется — она берётся из роста
цены, а не счёта.

### 14.1 Главный результат: при мгновенном перезаходе контур проигрывает всегда

Живое поведение ядра — перезаход через минуты (§11, гэпы около нуля), то есть
**пауза 0 ч**. В этом режиме:

| символ | холд ядра, net | контур (случайный триггер) | Δ | сидов лучше холда |
|---|---|---|---|---|
| ETHUSDT | +$298 | −$1 863 | **−$2 160** | **0 / 100** |
| BTCUSDT | +$271 | −$1 932 | **−$2 203** | **0 / 100** |
| SOLUSDT | +$413 | −$1 452 | **−$1 865** | **0 / 100** |

Отрицательно **во всех четырёх режимных корзинах** и на всех трёх символах;
доля эпизодов, где контур обошёл холд, — **0%**. Это не статистика, а
арифметика, и она доказана юнит-тестами: при перезаходе по цене фиксации
экспозиция не меняется, поэтому

`Δ = −2 × taker × нотионал × фиксаций + эффект пересчёта лота`

Проверка порядка: 1 249 фиксаций × 2 ноги × 0.055% × ≈$1 500 ≈ −$2 060 против
замеренных −$2 160 — остаток даёт просадка лота. Тесты фиксируют оба слагаемых
отдельно (постоянная цена → ровно 4 лишние ноги; вход 100 → фиксация 200 →
выход 300 → недобор −$50 на лоте).

**Следствие для гипотезы.** Формулировка §3 («печатает прибыль, пока лок выше
якоря») описывает не источник прибыли, а **переобозначение**: при мгновенном
перезаходе фиксация выше якоря просто перекладывает нереализованный плюс ядра
в реализованный, оплачивая перекладывание комиссией. Живые +$618 и +$393 — это
ход ядра, изъятый из будущего результата холда, минус ноги.

### 14.2 Предсказание литературы о чопе — не подтвердилось

§13.1 давало проверяемое условие: механизм должен выигрывать в осциллирующем
рынке. Проверено напрямую (режим определяется **ex-ante** — доходность за
30 дней до старта эпизода, чтобы не подглядывать в исход):

| режим | ETH | BTC | SOL |
|---|---|---|---|
| чоп, пауза 0 ч | −$442 | −$894 | −$360 |
| чоп, пауза 24 ч | **+$141** | −$607 | −$600 |
| тренд вниз, пауза 24 ч | −$484 | **+$57** | **+$344** |
| тренд вверх, пауза 24 ч | −$270 | −$51 | −$451 |

В чопе плюс появляется только у ETH и только при сутках вне рынка; у BTC и SOL
там устойчивый минус. В аптренде минус везде — это ожидаемо и совпадает с
теорией. Вывод: **условие из §13.1 на нашей конфигурации не выполняется**,
потому что паузы у нас нет — а без паузы режим вообще ни при чём.

### 14.3 Пауза вне рынка помогает, но ни одна её настройка не устойчива

Пауза — единственный способ контура реально изменить экспозицию, и Δ растёт
монотонно с её длиной (ETH: −$2 160 → −$1 847 → −$1 498 → −$949 → −$750 для
0/1/4/12/24 ч). Но даже при сутках паузы случайный триггер обгоняет холд лишь
в **0–3% сидов**.

Детерминированные триггеры прогнаны сеткой 2 типа × 3 параметра × 5 пауз = 30
ячеек на символ. Результат сведён по символам специально, чтобы не выхватить
удачную ячейку:

**плюсовых на всех трёх символах — 0 из 30.** Лучшие по отдельности ячейки
меняют знак между символами: `atr_trail 3ATR` при паузе 12 ч даёт ETH +$740 и
SOL +$370, но BTC −$873; `pct_trail 2%` при 24 ч — ETH +$546 и SOL +$488 при
BTC −$460. При паузе 0 и 1 ч (фактическое поведение) минус во **всех**
ячейках и на всех символах.

### 14.4 Что это даёт по гейтам §8

- **Гейт §8.1 не пройден и провален по существу**, а не по недобору выборки:
  контур отстаёт от холда на 730 днях, трёх символах, ≥1 800 фиксациях и во
  всех режимах.
- **Гейт §8.2 переопределён количественно.** Планка для ордерфлоу-триггера
  теперь не «лучше случайного», а конкретная сумма: чтобы просто сравняться с
  холдом, триггер должен переиграть случайное размещение паузы на ≈$950 (ETH,
  пауза 12 ч) или ≈$750 (пауза 24 ч) за два года — при том что случайное
  размещение проигрывает в 97–100% сидов.
- **Шаг 5 (сайзинг) обязателен, а не опционален**: просадка лота даёт
  измеримую часть отставания и лечится независимо от триггера.

### 14.5 Оговорки, ослабляющие вывод (в сторону контура)

- **Слippage не моделируется** — учтена только taker-комиссия. В реальности
  каждая фиксация ещё и проскальзывает, поэтому настоящее отставание **больше**
  измеренного. Вывод консервативен.
- **Ордерфлоу-триггер на истории недоступен**: плотности стакана и sweep'ы за
  два года мы не храним. Поэтому §14 закрывает вопрос про механику и планку,
  но **не** отвергает возможность skill у живого триггера — это шаг 2 и он
  требует live-наблюдений.
- **Ядро одно и слабое** (SMA20/50 4h не прошло IS/OOS; здесь холд даёт
  +2.7…4.1% equity за два года). Отставание контура от слабого холда не
  означает, что оно исчезнет на сильном ядре — но и надежды на это нет,
  поскольку слагаемое комиссий от качества ядра не зависит.
- Один тариф (VIP 0, taker 0.055%) и одна биржа.

---

## 15. Шаг 2: есть ли skill у живого триггера (2026-08-20)

**Артефакты:** `scripts/hybrid_trigger_skill.py` (read-only),
`data/hybrid_trigger_skill_30d_out.txt` (основной, 30 дней),
`data/hybrid_trigger_skill_out.txt` (первый прогон, 7 дней), тесты
`tests/test_hybrid_trigger_skill.py` (9 шт., арифметика без сети).

**Что и зачем измеряется.** §14 показал: без паузы фиксация не меняет
экспозицию, поэтому единственная выжившая линия — «пауза + триггер, который
размещает её лучше случайного». Планка из §14.4 — переиграть случайное
размещение на ≈$950 за два года. На истории проверить нельзя (плотностей
стакана за 2 года нет), поэтому мера построена на live-ногах.

**Метод.** Из `execution/list` берутся закрытия лота, сделанные **не** ядром
(скальп или биржевой брекет; позиция реплеится по ногам, `execType=Funding`
отбрасывается). Для каждого закрытия считается ход цены вперёд на 1/4/12/24 ч
и **ранг этого хода** среди случайных часов в окне ±48 ч того же интервала. Так
из измерения убирается тренд: вопрос не «упала ли цена», а «упала ли она
сильнее, чем в обычный час рядом». 50% = случайное попадание.

### 15.1 Результат: триггер неотличим от подбрасывания монеты

Окно 2026-07-21 → 08-20, 67 закрытий на двух символах. Оба фетча (ноги и
часовые свечи) пагинируются, поэтому окно расширяется без накопителя — накопитель
не нужен, история перечитывается целиком.

**ETHUSDT, 42 закрытия:**

| Горизонт | n | доля лучше обычного часа | p | средний ход цены после | пауза дала бы |
|---|---|---|---|---|---|
| 1 ч | 42 | 45% | 0.16 | +0.18% | −$629 |
| 4 ч | 40 | 46% | 0.64 | +0.41% | −$1 699 |
| 12 ч | 39 | 48% | 0.05 | +0.56% | −$2 402 |
| 24 ч | 36 | 51% | 0.87 | +0.43% | −$1 155 |

**BTCUSDT, 25 закрытий:** 45% / 46% / 49% / 40% (p = 0.42 / 1.00 / 1.00 / 0.23),
пауза дала бы −$52 / −$44 / −$362 / −$669.

Доля 45–51% при планке 50% — это ровно случайное попадание. Ни на одном
горизонте, ни на одном символе триггер не выбирает моменты лучше обычного часа
рядом. Направление отклонения на разных горизонтах не согласовано, значимости
нет (единственное p = 0.05 на ETH 12 ч указывает в сторону **худших** моментов
и не подтверждается ни соседними горизонтами, ни BTC).

Пауза при этом стоила бы денег на всех горизонтах: −$44…−$2 402. Это ожидаемо —
период суммарно восходящий, а сидеть вне рынка в рост дорого; ранговая мера как
раз и нужна, чтобы этот тренд из оценки skill вычесть.

### 15.2 Первый прогон на 7 днях был артефактом малой выборки

На окне 08-13 → 08-20 (ETH n=12) доли были 34/25/31/35% с p = 0.01…0.04, то
есть выглядело как значимый **анти-skill**. На тройной выборке эффект исчез
(45–51%). Записано здесь специально: это готовый пример того, почему порог
`sample-size.mdc` существует и почему на n=12 нельзя было ни отключать, ни
править ничего.

### 15.3 Что это даёт по гейтам §8

- **Гейт §8.2 не пройден.** Планка из §14.4 — переиграть случайное размещение
  паузы на ≈$950 за два года. Измеренный триггер равен случайному, то есть
  переигрывает его на $0. Отставание контура от холда (§14) этим не лечится.
- **Оба слагаемых гипотезы теперь измерены и оба нулевые или отрицательные:**
  механика фиксации без паузы не меняет экспозицию и стоит комиссий (§14),
  а размещение паузы триггером не лучше случайного (§15).

### 15.4 Оговорки

- **События склеены.** 24 из 41 промежутка на ETH и 14 из 24 на BTC короче 12 ч,
  значит независимых наблюдений примерно вдвое меньше, чем n (≈20 и ≈11). До
  планки 100 (`sample-size.mdc`) по независимым событиям не дошли.
- **Режим один.** Тридцать дней без выраженного нисходящего участка. Именно в
  нисходящем режиме пауза могла бы окупаться (§14.3), и там триггер не проверен.
- **Триггер не чистый.** Большая часть событий — механические брекеты скальпа
  (стоп/тейк на чужом объёме), а не ордерфлоу-сигнал из §3. Строго говоря,
  измерен skill *текущего фактического* закрывателя, а не гипотетического
  триггера по плотностям стакана.

Поэтому статус гипотезы: край не найден ни в одном из двух слагаемых, но линия
«пауза + чистый ордерфлоу-триггер в нисходящем режиме» формально не закрыта.
Изменений в логику ботов не вносим (`no-data-fitting.mdc`); продолжаем
перечитывать окно тем же счётчиком по мере накопления независимых событий.

---

## 16. Шаги 4-5: ядро и сайзинг (2026-08-20)

**Артефакты:** `scripts/hybrid_core_select.py` (read-only),
`data/hybrid_core_select_out.txt` (BTC/ETH/SOL, отбор),
`data/hybrid_core_select_oos_out.txt` (XRP/BNB/ADA/DOGE/LINK/AVAX,
out-of-sample), тесты `tests/hybrid_core_select` — 22 шт., без сети.

Логика шага: фиксация и триггер края не дают (§14, §15), значит матожидание
может быть только в самом трендовом правиле ядра и в размере позиции. 1460 дней,
taker 0.055% на ногу, фактический funding, сигнал на close — сделка на open
следующего бара. Правила зафиксированы до прогона, каждое со ссылкой
(Murphy 1999, Faber 2007, Faith 2007, Moskowitz/Ooi/Pedersen 2012,
Liu/Tsyvinski 2021); окна не подбирались.

Бенчмарк — холд, потому что ядро существует ради удержания инвентаря. Гейт
объявлен до прогона: Sharpe выше холда на **всех** символах, перевес над холдом
положителен в обеих половинах периода, p ниже порога Бонферрони на число
испытаний.

### 16.1 Шаг 4: ни одно каноническое правило не проходит гейт

Отбор (BTC/ETH/SOL) выделил ровно одного кандидата — `D SMA50`: Sharpe выше
холда на всех трёх (1.10 против 0.76; 0.89 против 0.46; 0.83 против 0.67) и
просадка примерно вдвое меньше (−29% против −62%; −40% против −80%; −62% против
−84%). Формально гейт он всё равно не прошёл: перевес по сырой доходности менял
знак между половинами и значимости после Бонферрони нет.

**OOS на шести других символах кандидата не подтвердил:** `D SMA50` уступает
холду по Sharpe на XRP (0.17 против 0.57) и LINK (0.32 против 0.49). Faber
`D SMA200` уступает холду на четырёх из четырёх пригодных символов. Ни одно из
семи правил не прошло гейт ни в отборе, ни OOS.

Отдельный факт про текущее ядро: `4h SMA20/50` **уступает холду по Sharpe на
BTC (0.55 против 0.74) и ETH (0.16 против 0.41)**, а OOS — на BNB и LINK. То
есть правило, на котором стоит `swing-bot`, не только не бьёт холд, но и по
риску хуже него на половине символов.

Вывод шага 4: замена трендового правила ядра на другое каноническое не решает
проблему. Край в трендовом тайминге на этом наборе данных не обнаруживается.

### 16.2 Шаг 5: сайзинг по волатильности — единственный устойчивый результат

На неизменном правиле ядра сравнивались три схемы размера: доля счёта (как
сейчас, 15%), постоянный нотионал и цель по волатильности (нотионал ∝
40% годовых / реализованная вола, окно 60 дней, потолок 3× — MOP 2012).

Порядок **вол-цель > фикс. нотионал > доля счёта** по Sharpe выполнился на
**9 символах из 9** (3 отбора + 6 OOS). Просадка у вол-цели ниже, чем у доли
счёта, тоже на 9 из 9, часто вдвое:

| Символ | Sharpe доля → вол-цель | Просадка доля → вол-цель |
|---|---|---|
| BTC | +0.57 → +0.85 | −6.1% → −5.5% |
| ETH | +0.22 → +0.39 | −14.0% → −8.2% |
| SOL | +0.91 → +1.02 | −11.6% → −4.3% |
| XRP | +0.86 → +1.02 | −15.3% → −12.5% |
| BNB | +0.56 → +0.63 | −11.1% → −9.4% |
| ADA | +0.30 → +0.71 | −21.0% → −10.5% |
| DOGE | +0.71 → +0.90 | −12.2% → −6.6% |
| LINK | +0.42 → +0.77 | −14.2% → −6.3% |
| AVAX | +0.41 → +0.64 | −21.1% → −10.5% |

Побочный эффект, важный для исходной жалобы на комиссии: вол-цель снижает и
издержки — комиссии на 8-50% и funding в тех же пропорциях (LINK: $169 → $87
комиссий, $385 → $198 funding). Меньше размера в волатильные периоды = меньше
оборот.

**Чего этот результат НЕ означает.** Это не новый край и не больше денег:
абсолютная доходность у вол-цели часто **ниже** (SOL +25.9% против +47.4%) —
плата за меньший размер в волатильные периоды. Утверждение строго такое: тот же
сигнал доставляется с меньшим риском и меньшими издержками. Часть согласованности
9/9 к тому же механическая — уменьшение размера при росте волы снижает просадку
по построению; нетривиальна только часть про Sharpe, и она воспроизводит
известный результат MOP 2012, а не открывает новый.

### 16.3 Что это меняет в статусе гипотезы

Все четыре измеренных слагаемых H-HYBRID дали отрицательный или нулевой
результат: механика фиксации (§14), skill триггера (§15), выбор трендового
правила ядра (§16.1). Положительный — только сайзинг (§16.2), и он к контуру
отношения не имеет: работает на голом ядре и улучшает риск, а не доходность.

Изменение сайзинга — правка торговой логики, поэтому требует явного одобрения
(`strategy-guard.mdc`) и здесь только предложено, не внесено. Перед внедрением
нужно: forward-test на демо, проверка на живых данных ядра и запись результата
сюда.

---

## 17. Разбор тех самых сделок и правила из них (2026-08-20)

**Артефакты:** `scripts/hybrid_event_ledger.py` (read-only),
`data/hybrid_event_ledger_out.txt`. Разобраны все 11 событий крупнее $20 по
эфиру за 21.07–20.08, включая присланные пользователем: +$618.83 (#4266),
+$393.95 (#4288), +$130.76 (#4276), +$40.61 (#4220), +$23.69 (#4218).

Раздел написан простыми словами намеренно: это описание механизма и правила,
а не оценка «хорошо/плохо» и не сравнение с другими вариантами.

### 17.1 Как получается большой плюс

Разбор события 6 (то самое +$618.83 в телеграме):

| Что | Значение |
|---|---|
| Позиция на бирже | покупка 5.140 ETH, средняя цена входа 1975.12 |
| Объём свинга | 3.560 ETH, куплен по 1918.20 (69% лота) |
| Объём скальпа | 1.580 ETH, куплен по 2103.36 (31% лота) |
| Кто закрыл | стоп скальпа, по цене 2097.02 |
| Расстояние до среднего входа | +6.17% |
| Деньги | +$626.58 (в телеграме $618.83 — разница это комиссии) |
| Зашли снова | через 1 минуту, 3.320 ETH по 2092.21 (−0.23%) |

Цепочка ровно такая:

1. Свинг купил заранее и дешёво, и его цена входа не меняется, пока цена растёт.
2. Скальп добавил сверху примерно треть объёма, уже по дороже.
3. Биржа держит **одну** позицию с одной средней ценой. Свинг держит больше
   и купил дешевле, поэтому средняя цена стоит заметно ниже рынка.
4. Скальп закрывается по своей причине. Его стоп и тейк висят на **всём** лоте,
   поэтому закрывается всё целиком.
5. Деньги = (цена закрытия − средняя цена входа) × весь лот. Средняя ниже рынка,
   поэтому почти любое закрытие выходит в плюс: 10 событий из 11 плюсовые.
6. Свинг заходит обратно за 0–2 минуты почти по той же цене, и позиция живёт
   дальше — просто объём чуть меньше.

### 17.2 Плюс даёт объём свинга; добавка скальпа его слегка разбавляет

Это измерено по каждому событию отдельно. В крупных случаях объём свинга **сам
по себе** дал бы больше, чем вышло по факту:

| Событие | Свинг сам по себе | Скальп сам по себе | Вышло по факту |
|---|---|---|---|
| 6 (19.08 16:06) | +$636.60 | −$10.02 | +$626.58 |
| 8 (20.08 07:17) | +$410.19 | −$9.70 | +$400.49 |
| 9 (20.08 09:16) | +$104.71 | −$10.21 | +$94.51 |

Причина простая: скальп покупает по текущей цене, то есть выше средней, и этим
поднимает среднюю цену всего лота. Расстояние до цены закрытия сокращается.

**Отсюда главный вывод для стратегии: объём второго бота не нужен. Нужно только
действие — закрытие всего лота целиком.**

### 17.3 Что показали все 11 событий

- Закрытия в плюс случались на +0.62%…+6.17% в нашу сторону от средней цены
  входа, серединное значение +0.91%.
- Держали до закрытия от 1.3 до 19.9 часа, серединное 3.7 часа.
- Размер лота в плюсовых событиях: от 1.76 до 5.14 ETH.
- Сумма по всем 11 событиям: +$1 415.97.
- Закрывал всегда скальп: своим выходом 6 раз, своим стопом 4 раза, своим
  тейком 1 раз. Свинг не закрыл ни разу.
- Заход обратно: 0–2 минуты и цена в пределах ±0.3% от закрытия (единственное
  исключение — событие 4, где пауза вышла 645 минут).

Размер плюса — это две величины, перемноженные: расстояние от средней цены и
размер лота. Самое крупное событие сложилось из обоих максимумов сразу: лот
5.14 ETH и расстояние +6.17%.

### 17.4 Правила стратегии

Ровно то, что происходит сейчас, но осознанно и одним ботом:

1. **Держим покупку**, пока трендовое правило говорит «покупать» (сейчас это
   SMA20/50 на 4-часовых свечах у свинга). Это единственный источник денег:
   цена входа остаётся низкой, пока рынок растёт.
2. **Ведём среднюю цену входа** по всей позиции. Это опорная величина: знак
   результата фиксации определяется только ей.
3. **Закрываем всё целиком по рынку**, когда цена ушла в нашу сторону не меньше
   чем на выбранный порог. Наблюдённые значения: серединное +0.91%, максимум
   +6.17%.
4. **Сразу заходим обратно** тем же объёмом, если трендовое правило всё ещё
   говорит «покупать». По факту это укладывается в 0–2 минуты и ±0.3% по цене.
5. **Повторяем.**

Второй бот и его объём в правилах не нужны (§17.2).

### 17.5 Что нужно решить и что за это платим

**Решить надо одно — порог из пункта 3.** Данные показывают развилку: чем больше
расстояние, тем крупнее событие, но и ждать дольше. Крайние примеры из выборки:
+0.91% дало $94.51, +6.17% дало $626.58. Выбор порога — это выбор между
«часто и по мелочи» и «редко и крупно».

**Плата за круг** (измерено): две комиссии, около $5.7 на текущем размере лота
по эфиру, плюс объём при обратном заходе становится меньше, если цена за это
время выросла (было 3.56 ETH, стало 3.32, потом 3.27, потом 3.11).

**Что уже измерено и в правила не входит:** момент закрытия угадывать не нужно
и не получается (§15) — знак результата задаёт средняя цена входа, а не удачный
выбор минуты.

### 17.6 Порог посчитан на истории эфира (2026-08-20)

**Артефакты:** `scripts/hybrid_fix_threshold.py`,
`data/hybrid_fix_threshold_out.txt`, тесты `tests/test_hybrid_fix_threshold.py`
(10 шт., без сети).

Прогнаны правила §17.4 на 1460 днях эфира, объём позиции $7 000 (примерно то,
чем торговал свинг в разобранных событиях), комиссия 0.055% с каждой ноги.
Заявка на закрытие считается сработавшей по максимуму свечи, обратный вход —
сразу по цене закрытия.

| Порог | Закрытий по порогу | В месяц | В каждом | Ждали | По порогу | По правилу (102 шт) | Комиссии | На руки |
|---|---|---|---|---|---|---|---|---|
| +0.5% | 1498 | 31.2 | $35 | 0 ч | +$52 430 | −$48 760 | −$12 322 | −$8 652 |
| +1% | 731 | 15.2 | $70 | 4 ч | +$51 170 | −$47 459 | −$6 416 | −$2 705 |
| +2% | 347 | 7.2 | $140 | 8 ч | +$48 580 | −$44 825 | −$3 459 | +$295 |
| +3% | 215 | 4.5 | $210 | 16 ч | +$45 150 | −$41 456 | −$2 443 | +$1 251 |
| +4% | 152 | 3.2 | $280 | 24 ч | +$42 560 | −$38 802 | −$1 958 | +$1 800 |
| +6% | 91 | 1.9 | $420 | 44 ч | +$38 220 | −$34 432 | −$1 488 | +$2 300 |
| +8% | 60 | 1.2 | $560 | 62 ч | +$33 600 | −$29 810 | −$1 249 | +$2 541 |
| без порога | — | — | — | — | — | — | −$788 | +$4 256 |

Что из таблицы следует прямо:

1. **Закрытия по порогу всегда в плюс, и их размер задан заранее** — это ровно
   порог, умноженный на объём. Именно эти цифры и приходят в телеграм.
2. **Убытки приходят отдельно**, когда позицию закрывает трендовое правило
   (102 раза за четыре года). Смотреть только на колонку «по порогу» нельзя:
   она всегда положительная по построению.
3. **Мелкие пороги вредны:** при +0.5% комиссии ($12 322) съедают всё и итог
   уходит в минус на $8 652.
4. **Чем больше порог, тем лучше итог и крупнее каждое закрытие.** Из
   посчитанных вариантов лучший — +8%.
5. **Ни один порог не обгоняет вариант «не закрывать вообще»** (+$4 256).
   Это зафиксировано как факт, решение за пользователем: он выбирает регулярную
   фиксацию сознательно.

Почему живое событие дало $626, а таблица обещает максимум $560 при +8%: в
живом случае совпали два максимума — лот был больше ($10 тыс. против $7 тыс.) и
расстояние успело набрать +6.17%, потому что фиксаций во время роста не было.
При мгновенном обратном входе средняя цена сбрасывается, поэтому расстояние
больше порога не бывает почти никогда.

---

## 18. Бот `hybrid_bot` (2026-08-20)

Правила §17.4 реализованы отдельным ботом `src/hybrid_bot/`. Он занял место
удалённого order-flow бота и наследует его ключи, том и Telegram-канал.

### 18.1 Что делает

Цикл раз в 3 минуты по каждому символу (`app/main.py`):

1. считает тренд по закрытым 4h барам: SMA20 > SMA50 → «держим покупку»;
2. если позиции нет и тренд вверх — покупает на `HYBRID_POSITION_USD`;
3. если позиция есть и цена ушла от средней цены входа на
   `HYBRID_FIX_THRESHOLD_PCT` — закрывает **свой** объём целиком и тут же
   заходит обратно;
4. если тренд развернулся — закрывает и остаётся вне рынка.

Решение вынесено в чистую функцию `plan()` — она и покрыта тестами
(`tests/test_hybrid_bot.py`, 23 шт., без сети и ключей).

### 18.2 Инварианты (не менять без обсуждения)

- **Закрывается только свой объём** — reduce-only на свою `qty`. Стоп или тейк
  на позицию (`tpslMode=Full`) не ставится **никогда**: такой ордер закрывает
  весь лот на счёте, включая объём других ботов. Именно из разбора этой путаницы
  выросла стратегия (§17.1), воспроизводить её нельзя. Метода для таких ордеров
  в клиенте нет физически.
- **Чужая позиция не трогается.** Если на символе есть лот, которого нет в БД
  бота, символ пропускается целиком.
- **Объём второго бота не нужен** (§17.2): бот не добавляет к позиции, вход
  всегда на полный нотионал, средняя цена = цена входа.
- **Стопа у стратегии нет.** В БД `sl` равен цене входа, поэтому tradecard не
  считает R-метрики, а не рисует бессмысленные значения.

### 18.3 Настройки

| Переменная | По умолчанию | Откуда значение |
|---|---|---|
| `HYBRID_TRADING_ENABLED` | `false` | наблюдение сначала: сделки пишутся с `mode=paper`, ордера не отправляются |
| `HYBRID_FIX_THRESHOLD_PCT` | `6.0` | §17.6 (лучший итог из посчитанных при разумном ожидании) |
| `HYBRID_VIRTUAL_CAPITAL` | `1000` | решение пользователя 2026-08-20: бот работает так, будто на счёте $1000 |
| `HYBRID_POSITION_USD` | `200` | решение пользователя 2026-08-20: максимальная ставка на позицию |
| `HYBRID_SYMBOLS` | `ETHUSDT` | §17: замер сделан на эфире |
| `HYBRID_INTERVAL` | `240` | трендовое правило на 4h |

Порог, капитал и ставка живут только в настройках: подбирать их в коде
запрещено (`no-data-fitting.mdc`). Суммарный объём позиций бота не превышает
`HYBRID_VIRTUAL_CAPITAL`, сколько бы ни лежало на счёте фактически.

### 18.5 Пересчёт замера §17.6 под ставку $200

Замер считался на нотионале $7 000 (столько торговал свинг в разобранных
сделках). Прогон заново со ставкой $200:
`python3 scripts/hybrid_fix_threshold.py --days 1460 --notional 200`,
вывод — `data/hybrid_fix_threshold_200_out.txt` (1460 дней ETHUSDT, 4h).

| Порог | В каждом закрытии | Закрытий в месяц | На руки за 4 года |
|---|---|---|---|
| +0.5% | $1 | 31.2 | −$246 |
| +1% | $2 | 15.2 | −$76 |
| +2% | $4 | 7.2 | +$10 |
| +3% | $6 | 4.5 | +$37 |
| +4% | $8 | 3.2 | +$53 |
| +6% | $12 | 1.9 | +$67 |
| +8% | $16 | 1.2 | +$74 |
| без порога | — | — | +$123 |

Все суммы — ровно 1/35 от замера на $7 000: комиссии пропорциональны объёму,
поэтому порядок порогов между собой не меняется. При ставке $200 порог +6%
даёт около $12 за фиксацию примерно дважды в месяц; крупные суммы из §17 (до
$626) на таком объёме недостижимы — они были следствием лота на $10 тыс.

### 18.4 Счёт: отдельный и пустой

Проверено `scripts/hybrid_account_check.py` (read-only) 2026-08-20:

| Счёт | Equity | Позиции |
|---|---|---|
| ключи предшественника (их наследует hybrid) | $44 557 | нет ни по ETH, ни по BTC, ни по SOL |
| ключи scalp (там же торгует swing) | $47 121 | ETH 3.10 @ 2285.51, BTC 0.106 @ 64209.30 |

Счета **разные**, и тот, что достаётся `hybrid_bot`, пуст: `fx_ai_trader`
остановлен, его позиций нет. Значит бот работает один, ни с кем позицию не
делит, и защита «не трогаем чужой лот» на практике срабатывать не будет —
она остаётся страховкой, а не рабочим режимом.

Практические следствия:

- Объём $7 000 из §17.6 — это ≈16% счёта в $44.5 тыс., то есть замер и живой
  счёт сопоставимы по масштабу. Менять объём под счёт не нужно.
- Никакой другой бот его позицию не закроет, поэтому фиксации будут случаться
  ровно по правилу порога, а не по чужому стопу. Это и есть та разница между
  случайным механизмом (§17.1) и стратегией.
