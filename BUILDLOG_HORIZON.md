# Build Log — daytrend-bot и swing-bot

Изолированы от `scalp_bot` / `flowzone` / advisor. Bybit linear, демо.

## 2026-08-20

### decision: контур H-HYBRID стал приоритетом; horizon = «ядро» контура
`9a017e1` — решение пользователя, код не менялся

Канон гипотезы вынесен в **`STRATEGY_HYBRID.md`**. Для horizon это значит:
трендовое правило (SMA20/50 4h у `swing-bot`) рассматривается как **ядро**
контура — источник матожидания. Шаги 4–5 плана исследования касаются именно
horizon: выбор ядра (SMA20/50 4h vs SMA200 daily, у которого есть 5-летнее
подтверждение на BTC) и сайзинг (убрать падение лота 15% equity / растущая
цена).

До прохождения гейтов §8 канона окна SMA, режим `broker_flat` и перезаход не
меняем — они нужны как есть, чтобы наблюдать контур.

**Файлы:** `STRATEGY_HYBRID.md` (новый), `BUILDLOG_HORIZON.md`

### research: бенчмарк — связка со скальпом отстаёт от простого холда ядра
`канон в BUILDLOG_SCALP.md`

Ядро ETH прошло 1902 → 2253 (+18.5%) за двое суток, SMA20/50 держала long
всё время. Скальп-брекеты снимали общий лот 6 раз (последние: #4276 Full-TP
@2120.91, #4288 Full-SL @2251.99 при ядре 3.27 @2126.86 → +$409 куска ядра).

Считано по всем реализациям одной книги: связка net **+$1160.26** (17 ног,
комиссия $59.56) против холда 3.59 @1902.27 net **+$1256.76** (1 нога, $3.76).
**Отставание −$96.51.** Причины: лишние ноги и падение лота ядра
3.59→3.11 (−13.4%), потому что 15% equity делится на растущую цену.
Перезаходы после лока: −0.024% / −0.235% / −0.022% / +0.281% / +0.062% —
преимущества нет.

Вывод для horizon: `broker_flat` + мгновенный перезаход **не** защита и не
край, это налог на тренд. Окна SMA не тюнить; ждём изоляции учёта на стороне
скальпа.

**Режим.** Весь отрезок — шорт-сквиз 19.08 (BTC >$70k впервые с 2 июня, ETH
>$2300, ликвидации шортов ~$2.7 млрд/24ч). Ядро попало в каскад, а не
предсказало его. Контроль: BTC-ядро 0.106 @ 64407.6 (01:02–05:58) закрылось
`broker_flat` **−$12.09**, скальп-тикет поверх него −$38.51 — тот же контур в
минусе, потому что якорь был выше цены фиксации.

`daytrend-bot` (SMA200 daily) в ETH и BTC оставался **флэт** всю ралли: в
логах только «чужая позиция … не трогаем». Два наших трендовых правила
разошлись, и всю экспозицию несло 4h SMA20/50 — то, у которого IS/OOS не
сошёлся. Сейчас ядро ETH 3.11 @ 2253.39 без стопа при рынке ~2247 (пик 2289):
якорь ≈ цена, асимметрия исчезла.

## 2026-08-19

### research(shared-account): H-SL-CONTAM — скальп-стоп закрыл ETH свинга
`наблюдение, код не менялся; канон в BUILDLOG_SCALP.md`

Полная гипотеза и арифметика — в `BUILDLOG_SCALP.md` (H-SL-CONTAM). Здесь
только сторона horizon, чтобы не потерять мелочи свинга.

**Что держал swing.** `swing-bot`, SMA20/50 4h, без своего SL/TP. ETH #2:
3.59 @ 1902.27 с 2026-08-18 11:19 UTC. Скальп #4218 Sell 1.73 в 15:41 **не
открыл шорт** — обрезал лонг до 1.86 (лог `broker=1.86` до 02:26). Horizon
это не `broker_flat`: size>0 и `ours` есть → hold, в БД qty так и 3.59.
Остаток 1.86 закрыл #4220 `flow_exit` в 07:14 (`pos.size=3.60` = 1.86+1.74).
Свинг `broker_flat` 07:16:55 по **всем** 3.59 @ last_price 1911, сразу #3:
3.56 @ 1918.2. Дальше #3 закрыл скальп-SL #4266. Ключи те же (`SCALP_BYBIT_*`).
Daytrend ETH не держал (skip «чужая 3.56 / 4.90»).

**Что случилось со свингом, не с его сигналом.** Скальп повесил Full-SL 2097
на всю one-way позицию. 16:06:38 UTC биржа закрыла 5.14 @ 2097.02. Свинг
через ~80 с увидел size=0 → `broker_flat`, exit **2084.94** (last_price, не
стоп 2097.02), pnl **+$593.59**. Реальный gross свинга на стопе был **+$636.60**.
SMA всё ещё long → сразу #4: 3.32 @ 2092.1 (15% уже от equity после +$620).
В 16:40 тот же механизм снял #4 (скальп #4267); свинг снова `broker_flat`
−$23.77 по last_price 2084.94 и сразу #5: 3.32 @ 2089.99 (открыт на момент
записи).

**Асимметрия.** Horizon чужой лот не трогает. Свой лот от чужого Full-SL не
защищён: стоп другого бота — это не «чужой ордер в стакане», а уровень на
общей позиции. `reduce_only` выход свинга шлёт `ours["qty"]`; до него дело
не дошло — позицию уже закрыл стоп скальпа.

**Учёт.** Выписка один раз: net +$620.65. Книги: скальп +618.83 + свинг
+593.59. Форвард SMA по ETH за 19.08 нельзя читать из `swing_bot.sqlite`
без вычищения `broker_flat` после скальп-стопа.

Код не менялся. Изоляция UID / запрет скальпу входить поверх `broker.size`
— решение в каноне SCALP, не тюнинг SMA.

### research: H-HYBRID — прибыль связки ядро+тактика+перезаход
`канон в BUILDLOG_SCALP.md`

Главная цель по трём тикетам — не «скальп в плюсе». Спутанный one-way
контур: SMA-ядро без стопа → ордерфлоу фиксирует кусок → при SMA long
сразу 15% снова. #4218/@1917 и #4220/@1918 не убили раннер именно из-за
перезахода #3; #4266 SL 2097 для ядра 1918 — лок **+9.3%**, не стоп −0.3%.
Сумма трёх кусков ~$698 при ~$15 комиссии. #4267 — тот же контур, якорь
свежий, уже минус. Не внедрять и не тюнить SMA, пока не будет явного
правила «фиксация только над якорем ядра» и форварда.

## 2026-08-18

### feat(horizon): два бота — дневной SMA200 и свинг 4h SMA 20/50
`7a5aad1`

Пользователь попросил рассмотреть свинг и запустить два бота по дневному
тренду и свингу.

**Дневной тренд на часах (ORB) не берём.** `scripts/scalp_daytrend_research.py`,
2 года 15м, VIP 0 0.110%: UTC ORB 15м n=647 ср −0.17% CI без нуля, обе
половины минус, медиана удержания 1.2ч, итог −67% на BTC. NY ORB шум около
нуля. Запускать это — торговать заведомо убыточное правило.

**Свинг рассмотрен, стат. гейт не пройден.** `scripts/scalp_swing_research.py`,
2 года: 4h Turtle 20/10, 10/5, SMA 20/50 и Daily SMA50. Удержание 1.8–6.2 дня
(это свинг), но IS/OOS знак разошёлся на всех правилах с n≥30. Край не
заявляется. На демо запускаем канон SMA 20/50 4h (Murphy 1999), чтобы копить
форвард, не подкручивая окна.

**Что запущено.** `daytrend-bot`: SMA200 daily long/flat (Murphy 1999),
артефакт `scripts/scalp_vip0_trend_research.py` (5 лет BTC +192% vs B&H +70%,
просадка −32% vs −67%). `swing-bot`: SMA20/50 на 4h. Оба: BTC+ETH, 15% equity
на символ, 1x, taker, чужие позиции scalp на том же демо не трогают.

**Файлы:** `src/horizon_bot/`, `Dockerfile.horizon-bot`, `docker-compose.yml`,
`tests/test_horizon_bot.py`, `STRATEGY_RATIONALE_HORIZON.md`,
`scripts/scalp_daytrend_research.py`, `scripts/scalp_swing_research.py`.
